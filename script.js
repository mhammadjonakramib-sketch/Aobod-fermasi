var LANG = 'lat';

var L = {
  lat: {
    ferma_nomi:'Akramjon Olimjon Obod F/X', login_err:"Login yoki parol noto'g'ri",
    login_label:'Foydalanuvchi nomi', parol_label:'Parol', kirish:'🔐 Kirish', chiqish:'Chiqish',
    nav_bosh:'Bosh', nav_sut:'Sut', nav_yem:'Yem', nav_mol:'Mollar', nav_sog:"Sog'liq",
    nav_kel:'Yem kelish', nav_ombor:'Ombor', nav_dar:'Daromad', nav_his:'Hisobot',
    bugungi_sut:'Bugungi sut', buzoglarga:"Buzog'larga", bugungi_yem:'Bugungi yem',
    jami_mol:'Jami mol', bu_oygi_sut:'Bu oygi sut', bu_oygi_yem:'Bu oygi yem',
    ortacha:"O'rtacha sut/kun", ortacha_bugun:"O'rtacha 1 mol/kun", songgi_sut:"So'nggi sut", songgi_yem:"So'nggi yem", rep_title:'Ferma hisoboti', rep_all:'Barcha davr', rep_gen:'Avtomatik yaratilgan hisobot', rep_s1:'Sut ishlab chiqarish', rep_s2:'Yem sarfi', rep_s3:'Daromad va tannarx', rep_s4:'Mol salomatligi', rep_s5:'Oylik voqealar', rep_jami_sut:'Jami sut', rep_komp:'Kompaniyaga', rep_tash:'Tashqariga', rep_buz:'Buzoqlarga', rep_ish:'Ishchilarga', rep_ort_kun:"O'rtacha/kun", rep_ort_bosh:"O'rtacha 1 bosh/kun", rep_faol:'Faol kunlar', rep_jami_yem:'Jami yem', rep_narx:'1 litr narxi', rep_yalpi:'Yalpi daromad', rep_xarajat:'Yem xarajati', rep_sof:'Sof foyda', rep_tannarx:'1 litr tannarx', rep_tugilgan:"Tug'ilgan buzoqlar", rep_davolangan:'Davolanganlar', rep_sogaygan:"Sog'ayganlar", rep_emlangan:'Emlanganlar', rep_kasal:'Kasal / davolanayotgan', rep_kutil:"Tug'ishi kutilayotgan", rep_yoq:"Yo'q", rep_kun:'kun', rep_ta:'ta', rep_otdi:"o'tdi", 
    yoq:"Hali yo'q", sana:'Sana', mol_soni:'Mol soni', bir_bosh:'1 bosh', ishchilarga:'Ishchilarga (L)', ishchi_q:'Ishchi', kechagidek:'📋 Kechagidek', jami:'Jami', mol:'Mol', davolanishdan_chiqdi:"Davolanishdan chiqdi", soglom_boldi:"Sog'lom bo'ldi", davolanishdan_chiqdi_msg:"davolanishdan chiqdi — Sog'lom qilindi", ortacha_mol_oy:"O'rtacha 1 mol/oy", jami_yem:'Jami yem', guruh_sigirlar:'Sigirlar', guruh_xonajinlar:'Xonajinlar', guruh_buzoqlar:'Buzoqlar', jami_berilgan:'Jami berilgan', bir_boshga:'1 boshga', bosh_q:'bosh', bosh_kiritilmagan:'bosh soni kiritilmagan', bugun_q:'bugun', yem_note:'suv litr=kg \u00b7 beda/somon 1 dona=1,5 kg', tur_soni:'Tur soni', jami_keldi:'Jami keldi', jami_sarf:'Jami sarf', kamaygan:'Kamaygan', tur_novvos:'Novvos', tur_sigir:'Sigir', tur_bezov:'Bezov', tur_xonajin:'Xonajin', opt_novvos:'🐂 Novvos', opt_sigir:'🐄 Sigir', opt_bezov:'🐃 Bezov', opt_xonajin:'🐮 Xonajin', opt_novvos_e:'🐂 Novvos (erkak)', opt_xonajin_u:"🐮 Xonajin (urg'ochi)", opt_bezov_u:"🐃 Bezov (urg'ochi)", opt_emlash:'💉 Emlash', opt_davolash:'💊 Davolash', opt_tugish:"🐣 Tug'ish", opt_vetkorik:"🩺 Vet. ko'rigi", opt_bogozlik:"🤰 Bo'g'ozlik", opt_boshqa:'📋 Boshqa', opt_1ta:'1 ta', opt_2ta:'2 ta (egizak)', ph_yosh:'3 yil', jami_sut:'Jami sut (L)',
    buzoglarga_l:"Buzog'larga (L)", tashqariga:'Tashqariga (L)', kompaniyaga:'Kompaniyaga (L)',
    saqlash:'💾 Saqlash', bekor:'Bekor', saqlandi:'Saqlandi!',
    yangi_sut:'Yangi sut yozuvi', sut_tahrir:'Sut yozuvini tahrirlash', yangilash:'✅ Yangilash',
    ochirish_tasdiq:"Rostdan ham o'chirilsinmi?",
    sut_yozuvlari:'Sut yozuvlari', guruh1:'1-guruh', guruh2:'2-guruh',
    sigirlar:'Sigirlar', xonajinlar:'Xonajinlar', buzoqlar:'Buzoqlar',
    ertalab:'Ertalab', kechqurun:'Kechqurun', yem_tarixi:'Yem tarixi',
    mol_qosh:"Mol qo'shish", yangi_mol:'Yangi mol', mol_raqami:'Mol raqami',
    mol_turi:'Mol turi', yoshi:'Yoshi', ogirligi:"Og'irligi (kg)",
    holati:'Holati', bogozlik_oyi:"Bo'g'ozlik oyi", hammasi:'Hammasi',
    soglom:"Sog'lom", bogoz:"Bo'g'oz", kasal:'Kasal', davolashda:'Davolashda',
    yangi_yozuv:'Yangi yozuv', voqea:'Voqea', dori_v:'Dori / Vaksina',
    tug_info:"Tug'ish ma'lumotlari", tug_tur:"Tug'ilgan tur", tug_jinsi:'Jinsi (1-bola)', tug_jinsi2:'Jinsi (2-bola)', bezov_jinsi:'Bezovlar jinsi', tug_soni:'Soni',
    tug_vazni:'Vazni (kg)', tug_raqami:'Yangi raqami',
    kutilgan:"Kutilgan tug'ish", doza:'Doza', keyingi:"Keyingi ko'rik", tavsif:'Tavsif',
    kel_sub:'Fermaga kelgan yemlarni yozish', yangi_kel:'Yangi yem keldi',
    yem_turi:'Yem turi', miqdori:'Miqdori', olchov:"O'lchov", narxi:"Narxi (so'm)",
    kim_keltirdi:'Kim keltirdi', kel_tarixi:'Kelgan yemlar',
    ombor_sub:'Keldi - Sarflandi = Qoldi', ombor_holat:'Ombor holati',
    keldi:'Keldi', sarflandi:'Sarflandi', qoldi:'Qoldi',
    sut_narxi:'Sut narxi', litr_narxi:"1 litr sut narxi (so'm)",
    oy_tanla:'Oy tanlang', kunlik_dar:'Kunlik daromad',
    ona_raqami:'Onasining raqami (ID)', narxi_birlik:"1 birlik narxi (so'm)", umumiy_summa:'Umumiy summa',
    emlash_alert1:'Diqqat! Bugun', emlash_alert2:"ni emlash/ko'rikdan o'tkazish kuni!",
    ombor_alert1:'Ogohlantirish: Omborda', ombor_alert2:'kam qoldi',
    roi_title:'Sut tannarxi va foyda kalkulyatori', roi_sotuv:"1 litr sut sotilish narxi (so'm)",
    roi_hint:'💡 Yem xarajati "Yem kelish"dagi narxlar asosida hisoblanadi. Tonna avtomatik kg ga aylantiriladi.',
    roi_yalpi:'Yalpi daromad', roi_xarajat:'Yem xarajati', roi_foyda:'Sof foyda', roi_tannarx:'1L sut tannarxi',
    roi_yem_sarf:'Sarflangan yem narxi', roi_foydali:'Foydali ✓', roi_zarar:'Zarar ✗', roi_per_litr:'Fermaga tushish narxi',
    grafik_sut:'Sut grafigi', grafik_yem:'Yem grafigi', grafik_ortacha:"O'rtacha sut (mol boshiga)",
    bezov_jinsi:'Bezov jinsi', bog_info:"Bo'g'ozlik ma'lumotlari",
    bog_hint:"💡 Sigir 9 oyda tug'adi. Oyni kiritsangiz, kutilgan tug'ish sanasi avtomatik hisoblanadi.",
    kunlik_sut:'Kunlik sut', yem_turlari:'Yem turlari',
    tashqariga_l:'Tashqariga', kompaniya_l:'Kompaniya',
    bildirishnomalar:'Bildirishnomalar', hammasi_oqildi:"Hammasini o'qildi", yangi_yoq:"Yangi bildirishnoma yo'q",
    hammasi_joyida:'Hammasi joyida', diqqat_yoq:"Diqqat talab qiladigan holat yo'q",
    pdf_hisobot:'PDF hisobot', excel_jadval:'Excel (CSV)', sotilgan:'Sotilgan', sotilgan_sana:'Sotilgan sana', sotildi_belgilash:'Sotildi', sotildi_tugma:'Sotildi', molni_sotish:'Molni sotish', sotish_tavsif:"Tavsif (kimga, narx, sabab)", sut_olchash:"Sut o'lchash", sut_olchov:"Sut o'lchovi", olchov_sana:"O'lchov sanasi", ertalab:"Ertalab", obed:"Obed", kechqurun:"Kechqurun", so_hint:"Har sigir uchun ertalab / obed / kechqurun sutini kiriting — raqamiga avtomatik saqlanadi", so_empty:"Sigir yo'q. Avval Mollar bo'limiga qo'shing.", so_done:"Bu sanada barcha sigirlar o'lchandi", so_saqlandi:"✅ Sut o'lchovlari saqlandi", sigirni_tanlang:"Sigirni tanlang", sigir_tanla:"Avval sigirni tanlang", sut_kiriting:"Kamida bitta vaqt sutini kiriting", opt_urugqoyish:"💉 Urug' qo'yish", urug_info:"Urug' qo'yish ma'lumotlari", urug_hint:"💡 45 kundan keyin \"tekshirilsinmi\" bildirishnomasi keladi. Doktor tasdiqlasa, bo'g'ozlik hisobi shu sanadan boshlanadi.", bogoz_ha:"Ha, bo'g'oz", bogoz_yoq:"Yo'q, qochdi", bogoz_tasdiqlandi:"✅ Bo'g'ozlik tasdiqlandi", bogoz_bekor:"Mol qaytadan urug' qo'yish uchun tayyor", sutdan_chiqarildi:"Sutdan chiqarildi", sutdan_chiqarildi_msg:"✅ Sog'ish to'xtatildi", kolostrum_ogoh:"Bu sigir yaqinda tug'gan — sut hali toza emas (og'iz suti)", sigir_reyting:"Sigirlar reytingi", sana_tanla:"Sana tanlang", rk_empty:"Bu sanada sut o'lchovi yo'q", rk_filter_empty:"Bu diapazonda mol yo'q", emlash_guruh_h:"Kimlarga emlanadi?", eg_hammasi:"Hammasi", eg_sigirlar:"Sigirlar", eg_novvoslar:"Novvoslar", eg_bezovlar:"Bezovlar", eg_xonajinlar:"Xonajinlar", eg_bogozlar:"Bo'g'ozlar", eg_bosh:"Bu guruhda mol yo'q", emlandi_msg:"emlandi", yopish:"Yopish", asosiy_malumot:"Asosiy ma'lumot", tur_q:"Turi", tugilgan_sana:"Tug'ilgan sana", onasi:"Onasi", yoshi:"Yoshi", ogirligi:"Og'irligi", homiladorlik:"Homiladorlik", boshlangan_sana:"Boshlangan sana", joriy_oy:"Joriy oy", kutilgan_tugish:"Kutilgan tug'ish", songgi_tugish:"So'nggi tug'ish", songgi_3_olchov:"So'nggi 3 marta sut o'lchovi", olchov_yoq:"Hali o'lchov yo'q", soglik_tarixi:"Sog'liq tarixi", tarix_yoq:"Tarix yo'q", yana:"yana", sotilgan_malumoti:"Sotilgan ma'lumoti", tavsif:"Tavsif", mol_topilmadi:"Mol topilmadi", faqat_sigir_bogoz:"Faqat Sigir yoki Xonajin bo'g'oz bo'lishi mumkin", tugish_tarixi:"Tug'ish tarixi", bolalari:"Bolalari", tahrirlash:"Tahrirlash", raqam_band:"Bu raqam boshqa molda band", saqlandi:"Saqlandi", taxminiy:"taxminiy", poda_tarkibi:'Poda tarkibi', sut_dinamikasi:'Sut dinamikasi', songgi_7_kun:'7 kun', kechaga_nisbatan:'kecha', kecha_bilan_bir_xil:'kecha bilan bir xil', yangi:'yangi'
  },
  kir: {
    ferma_nomi:'Акрамжон Олимжон Обод Ф/Х', login_err:'Логин ёки парол нотўғри',
    login_label:'Фойдаланувчи номи', parol_label:'Парол', kirish:'🔐 Кириш', chiqish:'Чиқиш',
    nav_bosh:'Бош', nav_sut:'Сут', nav_yem:'Ем', nav_mol:'Моллар', nav_sog:'Соғлиқ',
    nav_kel:'Ем келиш', nav_ombor:'Омбор', nav_dar:'Даромад', nav_his:'Ҳисобот',
    bugungi_sut:'Бугунги сут', buzoglarga:'Бузоғларга', bugungi_yem:'Бугунги ем',
    jami_mol:'Жами мол', bu_oygi_sut:'Бу ойги сут', bu_oygi_yem:'Бу ойги ем',
    ortacha:'Ўртача сут/кун', ortacha_bugun:'Ўртача 1 мол/кун', songgi_sut:'Сўнгги сут', songgi_yem:'Сўнгги ем', rep_title:'Ферма ҳисоботи', rep_all:'Барча давр', rep_gen:'Автоматик яратилган ҳисобот', rep_s1:'Сут ишлаб чиқариш', rep_s2:'Ем сарфи', rep_s3:'Даромад ва таннарх', rep_s4:'Мол саломатлиги', rep_s5:'Ойлик воқеалар', rep_jami_sut:'Жами сут', rep_komp:'Компанияга', rep_tash:'Ташқарига', rep_buz:'Бузоқларга', rep_ish:'Ишчиларга', rep_ort_kun:'Ўртача/кун', rep_ort_bosh:'Ўртача 1 бош/кун', rep_faol:'Фаол кунлар', rep_jami_yem:'Жами ем', rep_narx:'1 литр нархи', rep_yalpi:'Ялпи даромад', rep_xarajat:'Ем харажати', rep_sof:'Соф фойда', rep_tannarx:'1 литр таннарх', rep_tugilgan:'Туғилган бузоқлар', rep_davolangan:'Даволанганлар', rep_sogaygan:'Соғайганлар', rep_emlangan:'Эмланганлар', rep_kasal:'Касал / даволанаётган', rep_kutil:'Туғиши кутилаётган', rep_yoq:'Йўқ', rep_kun:'кун', rep_ta:'та', rep_otdi:'ўтди', 
    yoq:'Ҳали йўқ', sana:'Сана', mol_soni:'Мол сони', bir_bosh:'1 бош', ishchilarga:'Ишчиларга (Л)', ishchi_q:'Ишчи', kechagidek:'📋 Кечагидек', jami:'Жами', mol:'Мол', davolanishdan_chiqdi:'Даволанишдан чиқди', soglom_boldi:'Соғлом бўлди', davolanishdan_chiqdi_msg:'даволанишдан чиқди — Соғлом қилинди', ortacha_mol_oy:'Ўртача 1 мол/ой', jami_yem:'Жами ем', guruh_sigirlar:'Сигирлар', guruh_xonajinlar:'Хонажинлар', guruh_buzoqlar:'Бузоқлар', jami_berilgan:'Жами берилган', bir_boshga:'1 бошга', bosh_q:'бош', bosh_kiritilmagan:'бош сони киритилмаган', bugun_q:'бугун', yem_note:'сув литр=кг \u00b7 беда/сомон 1 дона=1,5 кг', tur_soni:'Тур сони', jami_keldi:'Жами келди', jami_sarf:'Жами сарф', kamaygan:'Камайган', tur_novvos:'Новвос', tur_sigir:'Сигир', tur_bezov:'Безов', tur_xonajin:'Хонажин', opt_novvos:'🐂 Новвос', opt_sigir:'🐄 Сигир', opt_bezov:'🐃 Безов', opt_xonajin:'🐮 Хонажин', opt_novvos_e:'🐂 Новвос (эркак)', opt_xonajin_u:'🐮 Хонажин (урғочи)', opt_bezov_u:'🐃 Безов (урғочи)', opt_emlash:'💉 Эмлаш', opt_davolash:'💊 Даволаш', opt_tugish:'🐣 Туғиш', opt_vetkorik:'🩺 Вет. кўриги', opt_bogozlik:'🤰 Бўғозлик', opt_boshqa:'📋 Бошқа', opt_1ta:'1 та', opt_2ta:'2 та (эгизак)', ph_yosh:'3 йил', jami_sut:'Жами сут (Л)',
    buzoglarga_l:'Бузоғларга (Л)', tashqariga:'Ташқарига (Л)', kompaniyaga:'Компанияга (Л)',
    saqlash:'💾 Сақлаш', bekor:'Бекор', saqlandi:'Сақланди!',
    yangi_sut:'Янги сут ёзуви', sut_tahrir:'Сут ёзувини таҳрирлаш', yangilash:'✅ Янгилаш',
    ochirish_tasdiq:'Ростдан ҳам ўчирилсинми?',
    sut_yozuvlari:'Сут ёзувлари', guruh1:'1-гуруҳ', guruh2:'2-гуруҳ',
    sigirlar:'Сигирлар', xonajinlar:'Хонажинлар', buzoqlar:'Бузоқлар',
    ertalab:'Эрталаб', kechqurun:'Кечқурун', yem_tarixi:'Ем тарихи',
    mol_qosh:'Мол қўшиш', yangi_mol:'Янги мол', mol_raqami:'Мол рақами',
    mol_turi:'Мол тури', yoshi:'Ёши', ogirligi:'Оғирлиги (кг)',
    holati:'Ҳолати', bogozlik_oyi:'Бўғозлик ойи', hammasi:'Ҳаммаси',
    soglom:'Соғлом', bogoz:'Бўғоз', kasal:'Касал', davolashda:'Даволашда',
    yangi_yozuv:'Янги ёзув', voqea:'Воқеа', dori_v:'Дори / Вакцина',
    tug_info:'Туғиш маълумотлари', tug_tur:'Туғилган тур', tug_jinsi:'Жинси (1-бола)', tug_jinsi2:'Жинси (2-бола)', bezov_jinsi:'Безовлар жинси', tug_soni:'Сони',
    tug_vazni:'Вазни (кг)', tug_raqami:'Янги рақами',
    kutilgan:'Кутилган туғиш', doza:'Доза', keyingi:'Кейинги кўрик', tavsif:'Тавсиф',
    kel_sub:'Фермага келган емларни ёзиш', yangi_kel:'Янги ем келди',
    yem_turi:'Ем тури', miqdori:'Миқдори', olchov:'Ўлчов', narxi:'Нархи (сўм)',
    kim_keltirdi:'Ким келтирди', kel_tarixi:'Келган емлар',
    ombor_sub:'Келди - Сарфланди = Қолди', ombor_holat:'Омбор ҳолати',
    keldi:'Келди', sarflandi:'Сарфланди', qoldi:'Қолди',
    sut_narxi:'Сут нархи', litr_narxi:'1 литр сут нархи (сўм)',
    oy_tanla:'Ой танланг', kunlik_dar:'Кунлик даромад',
    ona_raqami:'Онасининг рақами (ID)', narxi_birlik:'1 бирлик нархи (сўм)', umumiy_summa:'Умумий сумма',
    emlash_alert1:'Диққат! Бугун', emlash_alert2:'ни эмлаш/кўрикдан ўтказиш куни!',
    ombor_alert1:'Огоҳлантириш: Омборда', ombor_alert2:'кам қолди',
    roi_title:'Сут таннархи ва фойда калкулятори', roi_sotuv:'1 литр сут сотилиш нархи (сўм)',
    roi_hint:'💡 Ем харажати "Ем келиш"даги нархлар асосида ҳисобланади. Тонна автоматик кг га айлантирилади.',
    roi_yalpi:'Ялпи даромад', roi_xarajat:'Ем харажати', roi_foyda:'Соф фойда', roi_tannarx:'1Л сут таннархи',
    roi_yem_sarf:'Сарфланган ем нархи', roi_foydali:'Фойдали ✓', roi_zarar:'Зарар ✗', roi_per_litr:'Фермага тушиш нархи',
    grafik_sut:'Сут графиги', grafik_yem:'Ем графиги', grafik_ortacha:'Ўртача сут (мол бошига)',
    bezov_jinsi:'Безов жинси', bog_info:'Бўғозлик маълумотлари',
    bog_hint:'💡 Сигир 9 ойда туғади. Ойни киритсангиз, кутилган туғиш санаси автоматик ҳисобланади.',
    kunlik_sut:'Кунлик сут', yem_turlari:'Ем турлари',
    tashqariga_l:'Ташқарига', kompaniya_l:'Компания',
    bildirishnomalar:'Билдиришномалар', hammasi_oqildi:'Ҳаммасини ўқилди', yangi_yoq:'Янги билдиришнома йўқ',
    hammasi_joyida:'Ҳаммаси жойида', diqqat_yoq:'Диққат талаб қиладиган ҳолат йўқ',
    pdf_hisobot:'PDF ҳисобот', excel_jadval:'Excel (CSV)', sotilgan:'Сотилган', sotilgan_sana:'Сотилган сана', sotildi_belgilash:'Сотилди', sotildi_tugma:'Сотилди', molni_sotish:'Молни сотиш', sotish_tavsif:"Тавсиф (кимга, нарх, сабаб)", sut_olchash:"Сут ўлчаш", sut_olchov:"Сут ўлчови", olchov_sana:"Ўлчов санаси", ertalab:"Эрталаб", obed:"Обед", kechqurun:"Кечқурун", so_hint:"Ҳар сигир учун эрталаб / обед / кечқурун сутини киритинг — рақамига автоматик сақланади", so_empty:"Сигир йўқ. Аввал Моллар бўлимига қўшинг.", so_done:"Бу санада барча сигирлар ўлчанди", so_saqlandi:"✅ Сут ўлчовлари сақланди", sigirni_tanlang:"Сигирни танланг", sigir_tanla:"Аввал сигирни танланг", sut_kiriting:"Камида битта вақт сутини киритинг", opt_urugqoyish:"💉 Уруғ қўйиш", urug_info:"Уруғ қўйиш маълумотлари", urug_hint:"💡 45 кундан кейин \"текширилсинми\" билдиришномаси келади. Доктор тасдиқласа, бўғозлик ҳисоби шу санадан бошланади.", bogoz_ha:"Ҳа, бўғоз", bogoz_yoq:"Йўқ, қочди", bogoz_tasdiqlandi:"✅ Бўғозлик тасдиқланди", bogoz_bekor:"Мол қайтадан уруғ қўйиш учун тайёр", sutdan_chiqarildi:"Сутдан чиқарилди", sutdan_chiqarildi_msg:"✅ Соғиш тўхтатилди", kolostrum_ogoh:"Бу сигир яқинда туғган — сут ҳали тоза эмас (оғиз сути)", sigir_reyting:"Сигирлар рейтинги", sana_tanla:"Сана танланг", rk_empty:"Бу санада сут ўлчови йўқ", rk_filter_empty:"Бу диапазонда мол йўқ", emlash_guruh_h:"Кимларга эмланади?", eg_hammasi:"Ҳаммаси", eg_sigirlar:"Сигирлар", eg_novvoslar:"Новвослар", eg_bezovlar:"Безовлар", eg_xonajinlar:"Хонажинлар", eg_bogozlar:"Бўғозлар", eg_bosh:"Бу гуруҳда мол йўқ", emlandi_msg:"эмланди", yopish:"Ёпиш", asosiy_malumot:"Асосий маълумот", tur_q:"Тури", tugilgan_sana:"Туғилган сана", onasi:"Онаси", yoshi:"Ёши", ogirligi:"Оғирлиги", homiladorlik:"Ҳомиладорлик", boshlangan_sana:"Бошланган сана", joriy_oy:"Жорий ой", kutilgan_tugish:"Кутилган туғиш", songgi_tugish:"Сўнгги туғиш", songgi_3_olchov:"Сўнгги 3 марта сут ўлчови", olchov_yoq:"Ҳали ўлчов йўқ", soglik_tarixi:"Соғлиқ тарихи", tarix_yoq:"Тарих йўқ", yana:"яна", sotilgan_malumoti:"Сотилган маълумоти", tavsif:"Тавсиф", mol_topilmadi:"Мол топилмади", faqat_sigir_bogoz:"Фақат Сигир ёки Хонажин бўғоз бўлиши мумкин", tugish_tarixi:"Туғиш тарихи", bolalari:"Болалари", tahrirlash:"Таҳрирлаш", raqam_band:"Бу рақам бошқа молда банд", saqlandi:"Сақланди", taxminiy:"тахминий", poda_tarkibi:'Пода таркиби', sut_dinamikasi:'Сут динамикаси', songgi_7_kun:'7 кун', kechaga_nisbatan:'кеча', kecha_bilan_bir_xil:'кеча билан бир хил', yangi:'янги'
  }
};

function tx(k){ return (L[LANG] && L[LANG][k]) || (L.lat[k]) || k; }

function applyLang(){
  document.querySelectorAll('[data-l]').forEach(function(el){
    var k = el.getAttribute('data-l');
    var v = tx(k);
    if(el.tagName === 'OPTION'){ el.textContent = v; return; }
    if(el.tagName === 'INPUT' || el.tagName === 'SELECT' || el.tagName === 'TEXTAREA') return;
    el.textContent = v;
  });
  document.querySelectorAll('[data-ph]').forEach(function(el){
    el.setAttribute('placeholder', tx(el.getAttribute('data-ph')));
  });
  buildKtur();
}

// ── Yem nomlari va birliklar tarjimasi ──
var FEED_KIR={'Silos Makka':'Силос Макка','Beda':'Беда','Somon':'Сомон','Soya Shrot':'Соя Шрот','Raps Shrot':'Рапс Шрот','Semichka Shrot':'Семичка Шрот','Arpa Yarma':'Арпа Ярма','Makka Yarma':'Макка Ярма','Profat':'Профат','Premiks':'Премикс','Soda':'Сода','Mel':'Мел','Tuz':'Туз','Suv':'Сув','Sut (buzoq)':'Сут (бузоқ)','Boshqa':'Бошқа'};
function fName(n){ return (LANG==='kir' && FEED_KIR[n]) ? FEED_KIR[n] : n; }
var UNIT_KIR={'kg':'кг','dona':'дона','litr':'литр'};
function uName(u){ return (LANG==='kir' && UNIT_KIR[u]) ? UNIT_KIR[u] : (u||'kg'); }
function esc(s){ return String(s==null?'':s).replace(/[&<>"']/g,function(c){ return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]; }); }
function num(id,min,max){ var el=document.getElementById(id); var n=parseFloat(el?el.value:''); if(isNaN(n)) return 0; if(min!=null&&n<min) n=min; if(max!=null&&n>max) n=max; return n; }
var KEL_FEEDS=['Silos Makka','Beda','Somon','Soya Shrot','Raps Shrot','Semichka Shrot','Arpa Yarma','Makka Yarma','Profat','Premiks','Soda','Mel','Tuz','Suv','Boshqa'];
function buildKtur(){
  var sel=document.getElementById('ktur'); if(!sel) return;
  var cur=sel.value;
  sel.innerHTML=KEL_FEEDS.map(function(n){ return '<option value="'+n+'">'+fName(n)+'</option>'; }).join('');
  if(cur) sel.value=cur;
}

function setLang(l){
  LANG = l;
  document.getElementById('btn-lat').className = 'lb' + (l === 'lat' ? ' on' : '');
  document.getElementById('btn-kir').className = 'lb' + (l === 'kir' ? ' on' : '');
  applyLang();
  try { localStorage.setItem('FL', l); } catch(e){}
  if(typeof CU!=='undefined' && CU){ try{ buildAllYem(); updateDash(); renderSut(); renderYemH(); renderMolGrid(); renderSog(); renderKel(); renderOmbor(); }catch(e){} }
}

document.getElementById('btn-lat').addEventListener('click', function(){ setLang('lat'); });
document.getElementById('btn-kir').addEventListener('click', function(){ setLang('kir'); });

var CU = null;

// ── YEM LISTS ──
var YS = [
  {k:'s0',n:'Silos Makka',i:'🌽',u:'kg'},{k:'s1',n:'Beda',i:'🌿',u:'dona'},
  {k:'s2',n:'Somon',i:'🌾',u:'dona'},{k:'s3',n:'Soya Shrot',i:'🟤',u:'kg'},
  {k:'s4',n:'Raps Shrot',i:'🟡',u:'kg'},{k:'s5',n:'Semichka Shrot',i:'🌻',u:'kg'},
  {k:'s6',n:'Arpa Yarma',i:'🌾',u:'kg'},{k:'s7',n:'Makka Yarma',i:'🌽',u:'kg'},
  {k:'s8',n:'Profat',i:'🔵',u:'kg'},{k:'s9',n:'Premiks',i:'💊',u:'kg'},
  {k:'s10',n:'Soda',i:'⚪',u:'kg'},{k:'s11',n:'Mel',i:'🪨',u:'kg'},
  {k:'s12',n:'Tuz',i:'🧂',u:'kg'},{k:'s13',n:'Suv',i:'💧',u:'litr'}
];
var YX = [
  {k:'x0',n:'Silos Makka',i:'🌽',u:'kg'},{k:'x1',n:'Beda',i:'🌿',u:'dona'},
  {k:'x2',n:'Somon',i:'🌾',u:'dona'},{k:'x3',n:'Arpa Yarma',i:'🌾',u:'kg'},
  {k:'x4',n:'Makka Yarma',i:'🌽',u:'kg'},{k:'x5',n:'Premiks',i:'💊',u:'kg'},
  {k:'x6',n:'Tuz',i:'🧂',u:'kg'},{k:'x7',n:'Suv',i:'💧',u:'litr'}
];
var YB = [
  {k:'b0',n:'Silos Makka',i:'🌽',u:'kg'},{k:'b1',n:'Beda',i:'🌿',u:'dona'},
  {k:'b2',n:'Sut (buzoq)',i:'🥛',u:'litr'},{k:'b3',n:'Arpa Yarma',i:'🌾',u:'kg'},
  {k:'b4',n:'Premiks',i:'💊',u:'kg'},{k:'b5',n:'Suv',i:'💧',u:'litr'}
];
var YEM_MAP = {sigir:YS, xonajin:YX, buzoq:YB};
var GURUH_LABEL = {sigir:'Sigirlar', xonajin:'Xonajinlar', buzoq:'Buzoqlar'};
var HOL_DISP = {Soglom:"Sog'lom", Bogoz:"Bo'g'oz", Kasal:'Kasal', Davolashda:'Davolashda', Tekshiruv:'Tekshiruv kutilmoqda'};
var SOG_DISP = {Emlash:'Emlash', Davolash:'Davolash', Tugish:"Tug'ish", VetKorik:"Vet. ko'rigi", Bogozlik:"Bo'g'ozlik", Boshqa:'Boshqa'};

// ── STORAGE ──
// ── DATA LAYER (Supabase + kesh) ──
var sb = null;
// ── Supabase ulanish (oldindan yozilgan) ──
var SB_DEFAULT_URL = 'https://lfgqpvpgzrsqknxrrnqc.supabase.co';
var SB_DEFAULT_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImxmZ3FwdnBnenJzcWtueHJybnFjIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODE2MjQxMzgsImV4cCI6MjA5NzIwMDEzOH0.mfCuZzBk6vQ_zstW7H_RQ22WoL_CzvnwMsNnBcgX1YI';
var DB = {FS:[], FY:[], FK:[], FM:[], FG:[]};
var _snap = {FS:[], FY:[], FK:[], FM:[], FG:[]};
var SETT = {};
var TBL = {FS:'sut', FY:'yem', FK:'kel', FM:'mol', FG:'sog'};
function _cp(a){ try{ return JSON.parse(JSON.stringify(a)); }catch(e){ return []; } }
function ld(k){ if(DB[k]) return DB[k]; try{ var v=localStorage.getItem(k); return v?JSON.parse(v):[]; }catch(e){ return []; } }
function ldO(k){ if(k==='FSET') return SETT||{}; try{ var v=localStorage.getItem(k); return v?JSON.parse(v):{}; }catch(e){ return {}; } }
function sv(k,v){
  if(k==='FNREAD'){ try{ localStorage.setItem(k,JSON.stringify(v)); }catch(e){} return; }
  if(k==='FSET'){ SETT=v||{}; syncSettings(); return; }
  if(TBL[k]){ DB[k]=v; syncTable(k, v); return; }
  try{ localStorage.setItem(k,JSON.stringify(v)); }catch(e){}
}
function syncTable(k, arr){
  if(!sb) return;
  var tbl=TBL[k], snap=_snap[k]||[], byId={}, ups=[], delIds=[], seen={};
  snap.forEach(function(r){ byId[r.id]=r; });
  arr.forEach(function(r){ seen[r.id]=1; var old=byId[r.id]; if(!old || JSON.stringify(old)!==JSON.stringify(r)) ups.push({cid:r.id, payload:r}); });
  snap.forEach(function(r){ if(!seen[r.id]) delIds.push(r.id); });
  _snap[k]=_cp(arr);
  if(ups.length) sb.from(tbl).upsert(ups, {onConflict:'cid'}).then(function(res){ if(res.error) syncErr(res.error); });
  if(delIds.length) sb.from(tbl).delete().in('cid', delIds).then(function(res){ if(res.error) syncErr(res.error); });
}
function syncSettings(){ if(!sb) return; sb.from('settings').upsert({k:'FSET', v:SETT}, {onConflict:'k'}).then(function(res){ if(res.error) syncErr(res.error); }); }
function syncErr(e){ try{ var box=document.getElementById('ntoasts'); if(!box) return; var el=document.createElement('div'); el.className='ntoast org'; el.innerHTML='<div class="ic">⚠️</div><div><div class="tt">Saqlashda muammo</div><div class="ts">'+((e&&e.message)||'Internetni tekshiring')+'</div></div>'; box.appendChild(el); setTimeout(function(){ el.remove(); },5000); }catch(_){} }
function initClient(url, key){ try{ if(!window.supabase) return false; sb = window.supabase.createClient(url, key); return true; }catch(e){ return false; } }
function loadAll(){
  var jobs = Object.keys(TBL).map(function(k){
    return sb.from(TBL[k]).select('cid,payload').then(function(res){
      if(res.error) throw res.error;
      DB[k] = (res.data||[]).map(function(r){ return r.payload; }).filter(Boolean);
      _snap[k] = _cp(DB[k]);
    });
  });
  jobs.push(sb.from('settings').select('k,v').then(function(res){ SETT={}; if(!res.error && res.data) res.data.forEach(function(r){ if(r.k==='FSET') SETT=r.v||{}; }); }).catch(function(){ SETT={}; }));
  return Promise.all(jobs);
}
function busy(on, t){ var b=document.getElementById('busy'); if(!b) return; if(t){ var bt=document.getElementById('busy-t'); if(bt) bt.textContent=t; } b.classList.toggle('on', !!on); }
function loginErr(msg){ var e=document.getElementById('lerr'); if(e){ e.textContent=msg; e.style.display='block'; e.style.background=''; e.style.color=''; e.style.borderColor=''; } }
function clrLoginErr(){ var e=document.getElementById('lerr'); if(e) e.style.display='none'; }
function cfgErr(msg){ var e=document.getElementById('cfg-err'); if(e){ e.textContent=msg; e.style.display='block'; } }
function roleLabel(r){ return r==='admin'?'Admin':(r==='doktor'?'Doktor':(r==='molboqar'?'Mol boqar':'⏳ Tasdiq kutilmoqda')); }
function show(id, disp){ var e=document.getElementById(id); if(e) e.style.display=disp; }
function showConfig(){
  show('ls','none'); show('app','none'); show('cfg','flex');
  var u=localStorage.getItem('SB_URL')||'', k=localStorage.getItem('SB_KEY')||'';
  var eu=document.getElementById('cfg-url'), ek=document.getElementById('cfg-key');
  if(eu) eu.value=u; if(ek) ek.value=k;
}
function saveConfig(){
  var u=(document.getElementById('cfg-url').value||'').trim().replace(/\/+$/,'');
  var k=(document.getElementById('cfg-key').value||'').trim();
  if(!u || !k){ cfgErr('URL va kalitni kiriting'); return; }
  if(u.indexOf('http')!==0){ cfgErr("URL https:// bilan boshlanishi kerak"); return; }
  localStorage.setItem('SB_URL', u); localStorage.setItem('SB_KEY', k);
  if(!initClient(u,k)){ cfgErr("Ulanib bo'lmadi (sahifani yangilang)"); return; }
  show('cfg','none'); show('ls','flex'); clrLoginErr();
}
async function boot(){
  var u=localStorage.getItem('SB_URL')||SB_DEFAULT_URL, k=localStorage.getItem('SB_KEY')||SB_DEFAULT_KEY;
  localStorage.setItem('SB_URL', u); localStorage.setItem('SB_KEY', k);
  if(!u || !k){ showConfig(); return; }
  if(!initClient(u,k)){ showConfig(); return; }
  try{ var res = await sb.auth.getSession(); if(res && res.data && res.data.session){ await afterLogin(res.data.session.user); return; } }catch(e){}
  show('cfg','none'); show('app','none'); show('ls','flex');
}

// ── AUTH WIRING ──
(function(){
  function on(id, ev, fn){ var e=document.getElementById(id); if(e) e.addEventListener(ev, fn); }
  on('login-btn','click', doLogin);
  on('lp','keydown', function(e){ if(e.key==='Enter') doLogin(); });
  on('logout-btn','click', doLogout);
  on('signup-btn','click', doSignup);
  on('to-signup','click', function(){ clrLoginErr(); show('login-box','none'); show('signup-box','block'); });
  on('to-login','click', function(){ clrLoginErr(); show('signup-box','none'); show('login-box','block'); });
  on('to-cfg','click', function(){ showConfig(); });
  on('cfg-save','click', saveConfig);
  on('np-save','click', changeMyPass);
})();

async function doLogin(){
  if(!sb){ loginErr('Avval ulanish sozlanmagan'); return; }
  var email=(document.getElementById('lu').value||'').trim().toLowerCase();
  var pass=document.getElementById('lp').value||'';
  if(!email||!pass){ loginErr('Email va parolni kiriting'); return; }
  clrLoginErr(); busy(true,'Kirilmoqda...');
  try{
    var res = await sb.auth.signInWithPassword({ email:email, password:pass });
    if(res.error){
      busy(false);
      console.error('[LOGIN XATO]', res.error.message, '| code:', res.error.status||res.error.code);
      if(/email not confirmed/i.test(res.error.message)) loginErr("Email tasdiqlanmagan. Pochtangizni tekshiring.");
      else if(/rate limit|too many/i.test(res.error.message)) loginErr("Juda ko'p urinish. Bir necha daqiqadan keyin qayta urining.");
      else loginErr("Email yoki parol noto'g'ri");
      return;
    }
    await afterLogin(res.data.user);
  }catch(e){ busy(false); console.error('[LOGIN EXCEPTION]', e); loginErr('Xatolik: '+((e&&e.message)||e)); }
}
async function doSignup(){
  if(!sb){ loginErr('Avval ulanish sozlanmagan'); return; }
  var name=(document.getElementById('su-n').value||'').trim().slice(0,60);
  var email=(document.getElementById('su-e').value||'').trim().toLowerCase();
  var pass=document.getElementById('su-p').value||'';
  if(!email||pass.length<6){ loginErr('Email va kamida 6 belgili parol kiriting'); return; }
  clrLoginErr(); busy(true,"Ro'yxatdan o'tilmoqda...");
  try{
    var res = await sb.auth.signUp({ email:email, password:pass, options:{ data:{ name:name||email } } });
    if(res.error){
      busy(false);
      console.error('[SIGNUP XATO]', res.error.message, '| status:', res.error.status, '| name:', res.error.name);
      loginErr(res.error.message);
      return;
    }
    if(res.data && res.data.session){ await afterLogin(res.data.session.user); return; }
    busy(false);
    show('signup-box','none'); show('login-box','block');
    var le=document.getElementById('lerr');
    if(le){ le.textContent='✅ Hisob yaratildi. Endi email va parol bilan kiring.'; le.style.display='block'; le.style.background='#dcfce7'; le.style.color='#15803d'; le.style.borderColor='#86efac'; }
  }catch(e){
    busy(false);
    console.error('[SIGNUP EXCEPTION]', e, '| message:', e&&e.message, '| name:', e&&e.name);
    var hint = (e&&e.message&&/fetch|network|failed/i.test(e.message)) ? " (Tarmoq/CSP muammosi bo'lishi mumkin — Supabase manziliga ulanib bo'lmadi)" : '';
    loginErr('Xatolik: '+((e&&e.message)||e)+hint);
  }
}
async function afterLogin(user){
  var role='pending', name=(user&&user.email)||'';
  try{ var res = await sb.from('profiles').select('name,role').eq('id', user.id).single(); if(res.data){ role=res.data.role||'pending'; name=res.data.name||name; } }catch(e){}
  CU = { email:(user&&user.email)||'', name:name, role:role, rl:roleLabel(role) };
  if(['admin','doktor','molboqar'].indexOf(CU.role)<0){ busy(false); CU=null; show('app','none'); show('ls','flex'); loginErr("Hisobingiz hali tasdiqlanmagan. Administrator bilan bog'laning."); return; }
  busy(true,"Ma'lumotlar yuklanmoqda...");
  try{ await loadAll(); }
  catch(e){ busy(false); show('ls','flex'); loginErr("Ma'lumot yuklanmadi: "+((e&&e.message)||e)); return; }
  show('cfg','none'); show('ls','none'); show('app','block');
  var un=document.getElementById('uname'), ur=document.getElementById('urole');
  if(un) un.textContent=CU.name; if(ur) ur.textContent=CU.rl;
  busy(false);
  applyPerm(); setDefs(); buildAllYem();
  updateDash(); renderSut(); renderYemH(); renderMolGrid(); renderSog(); renderKel(); renderOmbor();
  refreshNotifs(); /* login'da popup yo'q — faqat qo'ng'iroq badge */
}
async function doLogout(){
  try{ if(sb) await sb.auth.signOut(); }catch(e){}
  CU=null;
  show('app','none'); show('cfg','none'); show('ls','flex');
  var lp=document.getElementById('lp'); if(lp) lp.value='';
  clrLoginErr();
}
async function changeMyPass(){
  var p=document.getElementById('np-pass').value||'', msg=document.getElementById('np-msg');
  if(p.length<6){ msg.className='umsg err'; msg.textContent="Parol kamida 6 belgi bo'lsin"; return; }
  msg.className='umsg'; msg.textContent='';
  try{
    var res = await sb.auth.updateUser({ password:p });
    if(res.error){ msg.className='umsg err'; msg.textContent=res.error.message; return; }
    msg.className='umsg ok'; msg.textContent='✅ Parol yangilandi';
    document.getElementById('np-pass').value='';
  }catch(e){ msg.className='umsg err'; msg.textContent='Xatolik: '+((e&&e.message)||e); }
}
async function renderUsers(){
  var box=document.getElementById('users-list'), msg=document.getElementById('users-msg');
  if(!box) return;
  if(msg){ msg.className='umsg'; msg.textContent=''; }
  box.innerHTML='<div style="color:var(--tm);font-size:13px;padding:8px">Yuklanmoqda...</div>';
  try{
    var res = await sb.from('profiles').select('id,email,name,role').order('created_at',{ascending:true});
    if(res.error) throw res.error;
    var rows=res.data||[], isA = CU && CU.role==='admin';
    box.innerHTML = rows.map(function(u){
      var pend = u.role==='pending';
      var sel = isA
        ? '<select data-uid="'+u.id+'" class="urole-sel">'+['pending','molboqar','doktor','admin'].map(function(r){return '<option value="'+r+'"'+(u.role===r?' selected':'')+'>'+roleLabel(r)+'</option>';}).join('')+'</select>'
        : '<span style="padding:5px 10px;border-radius:7px;background:var(--gp);color:var(--gd);font-size:11px;font-weight:700">'+roleLabel(u.role)+'</span>';
      var del = (isA && !(CU && u.email===CU.email)) ? '<button class="del-user" data-uid="'+u.id+'" data-nm="'+esc(u.name||u.email||'')+'" title="O\'chirish" style="background:#fee2e2;color:#b91c1c;border:none;border-radius:8px;width:34px;height:34px;font-size:14px;cursor:pointer;margin-left:6px;flex-shrink:0">🗑</button>' : '';
      return '<div class="urow"'+(pend?' style="background:#fef9c3;border-left:3px solid #eab308"':'')+'><div class="ua">'+esc((u.name||u.email||'?').charAt(0).toUpperCase())+'</div><div class="un"><div class="n">'+esc(u.name||'—')+(pend?' <span style="font-size:10px;color:#a16207;font-weight:800">⏳ YANGI — rol bering</span>':'')+'</div><div class="e">'+esc(u.email||'')+'</div></div>'+sel+del+'</div>';
    }).join('') || '<div style="color:var(--tm);font-size:13px;padding:8px">Foydalanuvchi yo\'q</div>';
    if(isA){
      box.querySelectorAll('.urole-sel').forEach(function(s){
        s.addEventListener('change', async function(){
          var uid=s.getAttribute('data-uid'), nr=s.value;
          try{ var r2 = await sb.from('profiles').update({role:nr}).eq('id', uid); if(r2.error) throw r2.error; if(msg){ msg.className='umsg ok'; msg.textContent='✅ Rol berildi — endi u kira oladi'; } renderUsers(); }
          catch(e){ if(msg){ msg.className='umsg err'; msg.textContent='Xatolik: '+((e&&e.message)||e); } }
        });
      });
      box.querySelectorAll('.del-user').forEach(function(b){
        b.addEventListener('click', async function(){
          var uid=b.getAttribute('data-uid'), nm=b.getAttribute('data-nm')||'';
          if(!confirm('"'+nm+'" — o\'chirib yuborilsinmi? U boshqa kira olmaydi.')) return;
          if(!sb){ alert('❌ Server ulanmagan (sb yo\'q)'); return; }
          try{
            var rd=await sb.from('profiles').delete().eq('id', uid).select();
            if(rd.error){ alert('❌ Xato: '+rd.error.message+(rd.error.code?' (code: '+rd.error.code+')':'')); return; }
            if(!rd.data || rd.data.length===0){ alert('⚠️ O\'chirilmadi — 0 qator o\'chdi.\n\nSizning rolingiz: '+(CU&&CU.role)+'\nUID: '+uid+'\n\nSabab: profiles_delete policy hali yo\'q yoki role admin emas. Supabase SQL\'da tekshiring.'); return; }
            alert('✅ "'+nm+'" o\'chirildi'); renderUsers();
          }
          catch(e){ alert('❌ '+((e&&e.message)||e)); }
        });
      });
    }
  }catch(e){ box.innerHTML=''; if(msg){ msg.className='umsg err'; msg.textContent='Yuklanmadi: '+((e&&e.message)||e); } }
}

function applyPerm(){
  var r = CU.role, isA = r==='admin';
  var navAll = document.querySelectorAll('#navt .nt');
  if(isA){
    navAll.forEach(function(e){ e.style.display=''; });
    document.querySelectorAll('.admin-only').forEach(function(e){ e.style.display=''; });
  } else {
    var allow = ({
      molboqar:['dash','sut','yem','mol','sutolchov'],
      doktor:  ['dash','sut','kel','mol','sog','sutolchov']
    })[r] || ['dash'];
    navAll.forEach(function(e){
      var pg = e.getAttribute('data-pg');
      e.style.display = (allow.indexOf(pg)>=0) ? '' : 'none';
    });
    document.querySelectorAll('.admin-only').forEach(function(e){ e.style.display='none'; });
  }
  var so=document.getElementById('sc-ort'); if(so) so.style.display=isA?'':'none';
  var bs=document.getElementById('sc-bu-sut'); if(bs) bs.style.display=(r==='molboqar')?'none':'';
  var ss=document.getElementById('card-songgi-sut'); if(ss) ss.style.display=(r==='molboqar')?'none':'';
  var sf=document.getElementById('sut-fm'); if(sf) sf.style.display=(r==='molboqar')?'none':'';
  var by=document.getElementById('sc-bu-yem'); if(by) by.style.display=(r==='doktor')?'none':'';
  var mo9=document.getElementById('sc-mol-oy'); if(mo9) mo9.style.display=(r==='molboqar')?'none':'';
  showPg('dash');
}

// ── NAV ──
document.getElementById('navt').addEventListener('click', function(e){
  var btn = e.target.closest('.nt');
  if(!btn) return;
  var pg = btn.getAttribute('data-pg');
  if(pg) showPg(pg);
});

function showPg(id){
  document.querySelectorAll('.pg').forEach(function(p){ p.classList.remove('on'); });
  document.querySelectorAll('.nt').forEach(function(t){ t.classList.remove('on'); });
  var pg = document.getElementById('pg-'+id);
  if(pg) pg.classList.add('on');
  document.querySelectorAll('.nt').forEach(function(t){ if(t.getAttribute('data-pg')===id) t.classList.add('on'); });
  if(id==='dash') updateDash();
  if(id==='his'){ renderHis(); renderRanking('all'); }
  if(id==='dar') renderDar();
  if(id==='ombor') renderOmbor();
  if(id==='kel') renderKel();
  if(id==='sog') renderSog();
  if(id==='mol') renderMolGrid();
  if(id==='sutolchov') renderSutOlchov();
  if(id==='users') renderUsers();
  refreshNotifs();
}

function setDefs(){
  var now = new Date();
  var td = now.toISOString().slice(0,10);
  var mo = td.slice(0,7);
  ['s1d','yd','sgd','kd'].forEach(function(id){ var e=document.getElementById(id); if(e) e.value=td; });
  ['sut-fm','yem-fm','his-m','dar-m','kel-fm','ombor-m'].forEach(function(id){ var e=document.getElementById(id); if(e) e.value=mo; });
  document.getElementById('today-d').textContent = now.toLocaleDateString('uz-UZ',{weekday:'long',year:'numeric',month:'long',day:'numeric'});
  document.getElementById('sut-fm').addEventListener('change', renderSut);
  document.getElementById('yem-fm').addEventListener('change', renderYemH);
  document.getElementById('his-m').addEventListener('change', renderHis);
  document.getElementById('dar-m').addEventListener('change', renderDar);
  document.getElementById('narx').addEventListener('input', renderDar);
  document.getElementById('kel-fm').addEventListener('change', renderKel);
  document.getElementById('ombor-m').addEventListener('change', renderOmbor);
}

// ── SUT ──
var editSutId = null;

document.getElementById('add-sut1-btn').addEventListener('click', saveSut);
document.getElementById('sut-cancel-btn').addEventListener('click', cancelEditSut);

function saveSut(){
  var date = document.getElementById('s1d').value;
  var ms   = num('s1ms',0,100000);
  var buz  = num('s1b',0,100000);
  var tash = num('s1t',0,100000);
  var komp = num('s1k',0,100000);
  var ishchi = num('s1i',0,100000);
  var litr = komp + tash + buz + ishchi;  // ← AVTOMATIK HISOBLASH
  if(!date || litr<=0){ alert(tx('sana')+' va kompaniya/tashqari/buzuq/ishchi sutlarni kiriting!'); return; }
  var recs = ld('FS');
  if(editSutId !== null){
    for(var i=0;i<recs.length;i++){
      if(recs[i].id===editSutId){
        recs[i].date=date; recs[i].ms=ms; recs[i].litr=litr;
        recs[i].buz=buz; recs[i].tash=tash; recs[i].komp=komp; recs[i].ishchi=ishchi;
        break;
      }
    }
    editSutId = null;
  } else {
    recs.push({id:Date.now(), date:date, ms:ms, litr:litr, buz:buz, tash:tash, komp:komp, ishchi:ishchi, w:CU.name});
  }
  sv('FS', recs);
  clearSutForm();
  renderSut(); updateDash();
  showTmpAlert('pg-sut');
}

function clearSutForm(){
  ['s1ms','s1b','s1t','s1k','s1i'].forEach(function(id){ document.getElementById(id).value=''; });
  var now=new Date(); document.getElementById('s1d').value=now.toISOString().slice(0,10);
  editSutId = null;
  document.getElementById('sut-form-title').textContent = tx('yangi_sut');
  document.getElementById('add-sut1-btn').textContent = tx('saqlash');
  document.getElementById('sut-cancel-btn').style.display = 'none';
}

function cancelEditSut(){ clearSutForm(); }

function editSut(id){
  var recs = ld('FS');
  var r = null;
  for(var i=0;i<recs.length;i++){ if(recs[i].id===id){ r=recs[i]; break; } }
  if(!r) return;
  document.getElementById('s1d').value = r.date;
  document.getElementById('s1ms').value = r.ms||'';
  document.getElementById('s1b').value = r.buz||'';
  document.getElementById('s1t').value = r.tash||'';
  document.getElementById('s1k').value = r.komp||'';
  document.getElementById('s1i').value = r.ishchi||'';
  editSutId = id;
  document.getElementById('sut-form-title').textContent = tx('sut_tahrir');
  document.getElementById('add-sut1-btn').textContent = tx('yangilash');
  document.getElementById('sut-cancel-btn').style.display = '';
  document.getElementById('pg-sut').scrollIntoView({behavior:'smooth', block:'start'});
}

function delSut(id){
  if(!confirm(tx('ochirish_tasdiq'))) return;
  sv('FS', ld('FS').filter(function(r){ return r.id!==id; }));
  if(editSutId===id) clearSutForm();
  renderSut(); updateDash();
}

function renderSut(){
  var mo = document.getElementById('sut-fm').value;
  var rows = ld('FS');
  if(mo) rows = rows.filter(function(r){ return r.date.indexOf(mo)===0; });
  if(CU.role==='molboqar'){ var _td=new Date().toISOString().slice(0,10); rows = rows.filter(function(r){ return r.date===_td; }); }
  rows.sort(function(a,b){ return b.date<a.date?-1:b.date>a.date?1:b.id-a.id; });
  var tb = document.getElementById('sut-tb'); tb.innerHTML='';
  document.getElementById('sut-empty').style.display = rows.length?'none':'block';
  var canEdit = CU.role==='admin' || CU.role==='molboqar' || CU.role==='doktor';
  rows.forEach(function(r){
    var tr = document.createElement('tr');
    var acts = '';
    if(canEdit) acts += '<button class="bd" style="background:var(--gm);margin-right:4px" data-edit-sut="'+r.id+'">✏️</button>';
    if(CU.role==='admin') acts += '<button class="bd" data-del-sut="'+r.id+'">🗑</button>';
    tr.innerHTML = '<td><strong>'+fd(r.date)+'</strong></td>'
      +'<td>'+(r.ms||'—')+'</td>'
      +'<td><strong class="cg">'+r.litr+'L</strong></td>'
      +'<td>'+(r.ms>0?'<strong class="ca">'+(r.litr/r.ms).toFixed(1)+'L</strong>':'—')+'</td>'
      +'<td>'+(r.komp?r.komp+'L':'—')+'</td>'
      +'<td>'+(r.tash?'<span class="ca fw7">'+r.tash+'L</span>':'—')+'</td>'
      +'<td>'+(r.buz?'<span class="cr fw7">'+r.buz+'L</span>':'—')+'</td>'
      +'<td>'+(r.ishchi?'<span class="fw7">'+r.ishchi+'L</span>':'—')+'</td>'
      +'<td><span class="badge bgn">'+esc(r.w)+'</span></td>'
      +'<td style="white-space:nowrap">'+acts+'</td>';
    tb.appendChild(tr);
  });
}

document.getElementById('sut-tb').addEventListener('click', function(e){
  var eb = e.target.closest('[data-edit-sut]');
  if(eb){ editSut(parseFloat(eb.getAttribute('data-edit-sut'))); return; }
  var btn = e.target.closest('[data-del-sut]');
  if(btn) delSut(parseFloat(btn.getAttribute('data-del-sut')));
});

// ── YEM ──
function buildAllYem(){
  buildYemGrid('sigir', YS, 'yg-sigir');
  buildYemGrid('xonajin', YX, 'yg-xonajin');
  buildYemGrid('buzoq', YB, 'yg-buzoq');
}

function buildYemGrid(guruh, list, cid){
  var cont = document.getElementById(cid); cont.innerHTML='';
  list.forEach(function(yt){
    var d = document.createElement('div'); d.className='ybox';
    d.innerHTML = '<div class="ybh" data-ybox="'+yt.k+'">'
      +'<div class="ybt">'+yt.i+' '+fName(yt.n)+'</div>'
      +'<div style="display:flex;align-items:center;gap:7px">'
      +'<span class="ytot" id="ytot-'+yt.k+'">0 '+uName(yt.u)+'</span>'
      +'<span class="chev" id="chev-'+yt.k+'">▼</span>'
      +'</div></div>'
      +'<div class="ybd" id="ybd-'+yt.k+'">'
      +'<div class="yrow">'
      +'<div><label>🌅 '+tx('ertalab')+'</label><input type="number" id="ye-'+yt.k+'" placeholder="0" min="0" inputmode="numeric"></div>'
      +'<div><label>☀️ '+tx('obed')+'</label><input type="number" id="yo-'+yt.k+'" placeholder="0" min="0" inputmode="numeric"></div>'
      +'<div><label>🌙 '+tx('kechqurun')+'</label><input type="number" id="yk-'+yt.k+'" placeholder="0" min="0" inputmode="numeric"></div>'
      +'</div>'
      +'<div style="font-size:11px;color:var(--tm);text-align:right">'+tx('jami')+': <strong id="ysum-'+yt.k+'" class="ca">0 '+uName(yt.u)+'</strong></div>'
      +'</div>';
    cont.appendChild(d);
    d.querySelector('.ybh').addEventListener('click', function(){
      var k = this.getAttribute('data-ybox');
      document.getElementById('ybd-'+k).classList.toggle('on');
      document.getElementById('chev-'+k).classList.toggle('on');
    });
    function calcY(){
      var e=parseFloat(document.getElementById('ye-'+yt.k).value)||0;
      var o=parseFloat(document.getElementById('yo-'+yt.k).value)||0;
      var c=parseFloat(document.getElementById('yk-'+yt.k).value)||0;
      var tot=Math.round((e+o+c)*10)/10;
      document.getElementById('ysum-'+yt.k).textContent=tot+' '+uName(yt.u);
      document.getElementById('ytot-'+yt.k).textContent=tot+' '+uName(yt.u);
    }
    document.getElementById('ye-'+yt.k).addEventListener('input', calcY);
    document.getElementById('yo-'+yt.k).addEventListener('input', calcY);
    document.getElementById('yk-'+yt.k).addEventListener('input', calcY);
  });
}

document.getElementById('yem-tabs').addEventListener('click', function(e){
  var btn = e.target.closest('.tab');
  if(!btn) return;
  var g = btn.getAttribute('data-yg');
  if(!g) return;
  document.querySelectorAll('#yem-tabs .tab').forEach(function(b){ b.classList.remove('on'); });
  btn.classList.add('on');
  ['sigir','xonajin','buzoq'].forEach(function(x){
    document.getElementById('yg-'+x).style.display = x===g?'':'none';
  });
});

document.getElementById('save-yem-btn').addEventListener('click', saveAllYem);

function saveAllYem(){
  var date = document.getElementById('yd').value;
  if(!date){ alert(tx('sana')+' kiriting!'); return; }
  var msSig = parseInt(document.getElementById('ms-sig').value)||0;
  var msXon = parseInt(document.getElementById('ms-xon').value)||0;
  var msMap = {sigir:msSig, xonajin:msXon, buzoq:0};
  var recs = ld('FY'); var n=0;
  ['sigir','xonajin','buzoq'].forEach(function(g){
    YEM_MAP[g].forEach(function(yt){
      var e=parseFloat(document.getElementById('ye-'+yt.k).value)||0;
      var o=parseFloat(document.getElementById('yo-'+yt.k).value)||0;
      var c=parseFloat(document.getElementById('yk-'+yt.k).value)||0;
      if(e+o+c>0){
        recs.push({id:Date.now()+Math.random(), date:date, tur:yt.n, key:yt.k,
          guruh:GURUH_LABEL[g], ert:e, obed:o, kech:c, jami:Math.round((e+o+c)*10)/10, ms:msMap[g], w:CU.name, u:yt.u});
        n++;
      }
    });
  });
  if(!n){ alert('Kamida bitta kiriting!'); return; }
  sv('FY', recs);
  Object.keys(YEM_MAP).forEach(function(g){
    YEM_MAP[g].forEach(function(yt){
      var ea=document.getElementById('ye-'+yt.k); if(ea) ea.value='';
      var oa=document.getElementById('yo-'+yt.k); if(oa) oa.value='';
      var ka=document.getElementById('yk-'+yt.k); if(ka) ka.value='';
      var sa=document.getElementById('ysum-'+yt.k); if(sa) sa.textContent='0 '+uName(yt.u);
      var ta=document.getElementById('ytot-'+yt.k); if(ta) ta.textContent='0 '+uName(yt.u);
    });
  });
  document.getElementById('ms-sig').value='';
  document.getElementById('ms-xon').value='';
  renderYemH(); updateDash();
  alert('✅ '+n+' ta yem saqlandi!');
}

function delYem(id){
  sv('FY', ld('FY').filter(function(r){ return r.id!==id; }));
  renderYemH(); updateDash();
}

function renderYemH(){
  var mo = document.getElementById('yem-fm').value;
  var rows = ld('FY');
  if(mo) rows = rows.filter(function(r){ return r.date.indexOf(mo)===0; });
  var box=document.getElementById('yem-days'); box.innerHTML='';
  document.getElementById('yem-empty').style.display=rows.length?'none':'block';
  if(!rows.length) return;
  var byDate={};
  rows.forEach(function(r){ (byDate[r.date]=byDate[r.date]||[]).push(r); });
  var dates=Object.keys(byDate).sort().reverse();
  var gcls={Sigirlar:'bgn',Xonajinlar:'bam',Buzoqlar:'btl'};
  dates.forEach(function(d){
    var recs=byDate[d];
    var detail='';
    ['Sigirlar','Xonajinlar','Buzoqlar'].forEach(function(g){
      var gr=recs.filter(function(r){ return r.guruh===g; });
      if(!gr.length) return;
      detail+='<div class="dyg"><span class="badge '+(gcls[g]||'bgn')+'">'+g+'</span>'+(gr[0].ms?' · '+gr[0].ms+' bosh':'')+'</div>';
      gr.forEach(function(r){
        detail+='<div class="yhrow"><span>'+fName(r.tur)+'</span>'
          +'<span style="color:var(--tm)">🌅'+(r.ert||0)+' ☀️'+(r.obed||0)+' 🌙'+(r.kech||0)+' = <strong class="ca">'+r.jami+' '+uName(r.u)+'</strong></span>'
          +(CU.role==='admin'?'<button class="bd" data-del-yem="'+r.id+'">🗑</button>':'')+'</div>';
      });
    });
    var dayId='yday-'+d.replace(/-/g,'');
    var card=document.createElement('div'); card.className='ybox';
    card.innerHTML='<div class="ybh" data-day="'+dayId+'"><div class="ybt">📅 '+fd(d)+'</div>'
      +'<div style="display:flex;align-items:center;gap:7px"><span class="ytot">'+recs.length+' xil yem</span><span class="chev" id="chev-'+dayId+'">▼</span></div></div>'
      +'<div class="ybd" id="'+dayId+'">'+detail+'</div>';
    box.appendChild(card);
    card.querySelector('.ybh').addEventListener('click', function(){
      document.getElementById(dayId).classList.toggle('on');
      document.getElementById('chev-'+dayId).classList.toggle('on');
    });
  });
}

// "Kechagidek" — eng oxirgi kungi yemlarni formaga avtomatik yuklash
function copyYesterday(){
  var all = ld('FY');
  if(!all.length){ alert('Avval kamida bir kun yem kiriting'); return; }
  var dates = all.map(function(r){ return r.date; }).sort();
  var last = dates[dates.length-1];
  var recs = all.filter(function(r){ return r.date===last; });
  Object.keys(YEM_MAP).forEach(function(g){ YEM_MAP[g].forEach(function(yt){
    var ee=document.getElementById('ye-'+yt.k), oo=document.getElementById('yo-'+yt.k), kk=document.getElementById('yk-'+yt.k);
    if(ee) ee.value=''; if(oo) oo.value=''; if(kk) kk.value='';
  }); });
  recs.forEach(function(r){
    var ee=document.getElementById('ye-'+r.key), oo=document.getElementById('yo-'+r.key), kk=document.getElementById('yk-'+r.key);
    if(ee && r.ert) ee.value=r.ert;
    if(oo && r.obed) oo.value=r.obed;
    if(kk && r.kech) kk.value=r.kech;
    if(r.guruh==='Sigirlar' && r.ms) document.getElementById('ms-sig').value=r.ms;
    if(r.guruh==='Xonajinlar' && r.ms) document.getElementById('ms-xon').value=r.ms;
  });
  Object.keys(YEM_MAP).forEach(function(g){ YEM_MAP[g].forEach(function(yt){
    var e=parseFloat((document.getElementById('ye-'+yt.k)||{}).value)||0;
    var o=parseFloat((document.getElementById('yo-'+yt.k)||{}).value)||0;
    var c=parseFloat((document.getElementById('yk-'+yt.k)||{}).value)||0;
    var s=document.getElementById('ysum-'+yt.k), t=document.getElementById('ytot-'+yt.k);
    if(s) s.textContent=(e+o+c)+' '+uName(yt.u);
    if(t) t.textContent=(e+o+c)+' '+uName(yt.u);
  }); });
  alert("✅ "+fd(last)+" kungi yemlar yuklandi.\nSanani bugunga qo'yib, kerak bo'lsa o'zgartiring va Saqlang.");
}
var _cyBtn=document.getElementById('copy-yem-btn'); if(_cyBtn) _cyBtn.addEventListener('click', copyYesterday);

document.getElementById('yem-days').addEventListener('click', function(e){
  var btn=e.target.closest('[data-del-yem]');
  if(btn){ e.stopPropagation(); delYem(parseFloat(btn.getAttribute('data-del-yem'))); }
});

// ── MOL ──
document.getElementById('mol-form-btn').addEventListener('click', function(){
  var f=document.getElementById('mol-form');
  f.style.display=f.style.display==='none'?'':'none';
});
document.getElementById('mol-cancel-btn').addEventListener('click', function(){
  document.getElementById('mol-form').style.display='none';
});
document.getElementById('add-mol-btn').addEventListener('click', addMol);

// Bezov tanlanса jins qatori ko'rinadi, Bezov/Xonajin uchun ona raqami tavsiya etiladi
document.getElementById('mt').addEventListener('change', function(){
  document.getElementById('bezov-jins-row').style.display = this.value==='Bezov' ? '' : 'none';
  var isBezov = this.value==='Bezov';
  document.getElementById('yosh-row').style.display = isBezov ? 'none' : '';
  document.getElementById('tugilgan-row').style.display = isBezov ? '' : 'none';
  if(isBezov){ var td=document.getElementById('m-tug'); if(td && !td.value) td.value=new Date().toISOString().slice(0,10); }
  var boRow=document.getElementById('bogoz-oyi-row');
  if(boRow) boRow.style.display = (this.value==='Sigir' || this.value==='Xonajin') ? '' : 'none';
  var onaLabel = document.querySelector('label[data-l="ona_raqami"]');
  if(this.value==='Bezov'){
    onaLabel.style.color = '#dc2626';
    onaLabel.innerHTML = tx('ona_raqami')+' ⭐';
  } else {
    onaLabel.style.color = '';
    onaLabel.innerHTML = tx('ona_raqami');
  }
});

document.getElementById('mol-sg').addEventListener('click', function(e){
  var btn=e.target.closest('.sc');
  if(!btn) return;
  var f=btn.getAttribute('data-mf');
  if(!f) return;
  renderMolGrid(f);
});
(function(){ var ms=document.getElementById('mol-search'); if(ms) ms.addEventListener('input', function(){ molSearch=this.value; renderMolGrid(); }); })();

function addMol(){
  var raqam=document.getElementById('mr').value.trim().slice(0,20);
  if(!raqam){ alert(tx('mol_raqami')+' kiriting!'); return; }
  var tur = document.getElementById('mt').value;
  var bogozOy = parseInt(document.getElementById('mbo').value)||0;
  var holat = document.getElementById('mh').value;
  if(bogozOy>=1 && (tur==='Sigir'||tur==='Xonajin')) holat='Bogoz';
  var bugun = new Date().toISOString().slice(0,10);
  var tugSanaVal = document.getElementById('m-tug').value;
  var mol = {
    id:Date.now(), raqam:raqam, tur:tur,
    yosh:document.getElementById('my').value.slice(0,30), vazn:'',
    holat:holat, bogoz:document.getElementById('mbo').value,
    ona:document.getElementById('m-ona').value.trim().slice(0,20), q:bugun
  };
  if(tur==='Bezov'){
    mol.bezovJins = document.getElementById('bezov-jins').value;
    if(tugSanaVal){ mol.q = tugSanaVal; mol.tugganMi = true; mol.yosh=''; }
  }
  if(holat==='Bogoz' && bogozOy>=1){
    mol.bogozBosh = addMonths(bugun, -bogozOy);
    mol.kutilgan = addMonths(mol.bogozBosh, 9);
  }
  var mols=ld('FM'); mols.push(mol);
  sv('FM', mols);
  ['mr','my','mbo','m-ona','m-tug'].forEach(function(id){ document.getElementById(id).value=''; });
  document.getElementById('bezov-jins-row').style.display='none';
  document.getElementById('yosh-row').style.display='';
  document.getElementById('tugilgan-row').style.display='none';
  document.getElementById('mol-form').style.display='none';
  renderMolGrid(); updateDash();
  showTmpAlert('pg-mol');
}

function delMol(id){
  if(!confirm(tx('ochirish_tasdiq'))) return;
  sv('FM', ld('FM').filter(function(m){ return m.id!==id; }));
  renderMolGrid(); updateDash();
}

function milkBox(m){
  if(!(m.tur==='Sigir'||m.tur==='Xonajin') || !m.sutOlchov || !m.sutOlchov.length) return '';
  var last=m.sutOlchov.slice().sort(function(a,b){ return String(b.date||'').localeCompare(String(a.date||'')); }).slice(0,3);
  return '<div class="milkbox"><div class="milkt">🥛 '+tx('sut_olchov')+'</div>'
    + last.map(function(o){ var e=+o.ert||0,ob=+o.obed||0,k=+o.kech||0,t=Math.round((e+ob+k)*10)/10;
        return '<div class="milkrow"><span>'+fd(o.date)+' <span style="color:var(--tm);font-size:10px">🌅'+e+' ☀️'+ob+' 🌙'+k+'</span></span><strong>'+t+' L</strong></div>'; }).join('')
    + '</div>';
}
var curMolFilter = 'all';
var molSearch = '';
function renderMolGrid(filter){
  if(filter !== undefined) curMolFilter = filter;
  var mols=ld('FM');
  var active=mols.filter(function(m){ return !m.sold; });
  var fl=active;
  if(curMolFilter==='sold') fl=mols.filter(function(m){ return m.sold; });
  else if(curMolFilter==='Bogoz') fl=active.filter(function(m){ return m.holat==='Bogoz'; });
  else if(curMolFilter==='Kasal') fl=active.filter(function(m){ return m.holat==='Kasal'; });
  else if(curMolFilter==='Davolashda') fl=active.filter(function(m){ return m.holat==='Davolashda'; });
  else if(curMolFilter!=='all') fl=active.filter(function(m){ 
    if(m.tur!==curMolFilter) return false;
    if(curMolFilter==='Sigir' && m.holat==='Bogoz'){
      var oy = m.bogozBosh ? hozirgiBogozOy(m.bogozBosh) : (parseInt(m.bogoz)||0);
      if(oy >= 7) return false;
    }
    return true;
  });
  var _q=(molSearch||'').trim().toLowerCase();
  if(_q) fl=fl.filter(function(m){ return String(m.raqam||'').toLowerCase().indexOf(_q)>=0; });
  var tot=active.length,nov=0,sig=0,bez=0,xon=0,bog=0,kas=0,dav=0,bezN=0,bezX=0;
  active.forEach(function(m){
    if(m.tur==='Novvos') nov++;
    if(m.tur==='Sigir') sig++;
    if(m.tur==='Bezov'){ bez++; if(m.bezovJins==='Novvos') bezN++; else if(m.bezovJins==='Xonajin') bezX++; }
    if(m.tur==='Xonajin') xon++;
    if(m.holat==='Bogoz') bog++;
    if(m.holat==='Kasal') kas++;
    if(m.holat==='Davolashda') dav++;
  });
  var sold = mols.filter(function(m){ return m.sold; }).length;
  function st(f){ return curMolFilter===f ? 'cursor:pointer;box-shadow:0 0 0 2.5px var(--gm) inset;background:rgba(216,243,220,0.8);' : 'cursor:pointer;'; }
  document.getElementById('mol-sg').innerHTML=
    '<div class="sc so" data-mf="all" style="'+st('all')+'"><div class="si">🐄</div><div class="sv">'+tot+'</div><div class="sl">'+tx('jami')+'</div></div>'
    +'<div class="sc gn" data-mf="Novvos" style="'+st('Novvos')+'"><div class="si">🐂</div><div class="sv">'+nov+'</div><div class="sl">'+tx('tur_novvos')+'</div></div>'
    +'<div class="sc gn" data-mf="Sigir" style="'+st('Sigir')+'"><div class="si">🐄</div><div class="sv">'+sig+'</div><div class="sl">'+tx('tur_sigir')+'</div></div>'
    +'<div class="sc tl" data-mf="Bezov" style="'+st('Bezov')+'"><div class="si">🐃</div><div class="sv">'+bez+'</div><div class="sl">'+tx('tur_bezov')+'</div></div>'
    +'<div class="sc am" data-mf="Xonajin" style="'+st('Xonajin')+'"><div class="si">🐮</div><div class="sv">'+xon+'</div><div class="sl">'+tx('tur_xonajin')+'</div></div>'
    +'<div class="sc pu" data-mf="Bogoz" style="'+st('Bogoz')+'"><div class="si">🤰</div><div class="sv">'+bog+"</div><div class=\"sl\">"+tx('bogoz')+"</div></div>"
    +'<div class="sc rd" data-mf="Kasal" style="'+st('Kasal')+'"><div class="si">⚠️</div><div class="sv">'+kas+'</div><div class="sl">'+tx('kasal')+'</div></div>'
    +'<div class="sc am" data-mf="Davolashda" style="'+st('Davolashda')+'"><div class="si">💊</div><div class="sv">'+dav+'</div><div class="sl">'+tx('davolashda')+'</div></div>'
    +'<div class="sc so" data-mf="sold" style="'+st('sold')+'"><div class="si">💰</div><div class="sv">'+sold+'</div><div class="sl">'+tx('sotilgan')+'</div></div>';
  var _bd=document.getElementById('mol-bezbd');
  if(_bd) _bd.innerHTML = (curMolFilter==='Bezov' && bez>0) ? '<div class="card" style="padding:12px 14px;margin-bottom:12px"><div style="font-size:13px;font-weight:700;color:var(--gm);margin-bottom:6px">🐃 '+tx('bezov_jinsi')+'</div><div style="font-size:14px">🐂 <strong>'+bezN+'</strong> '+tx('tur_novvos')+' \u00b7 🐮 <strong>'+bezX+'</strong> '+tx('tur_xonajin')+'</div></div>' : '';
  var grid=document.getElementById('mol-grid'); grid.innerHTML='';
  document.getElementById('mol-empty').style.display=fl.length?'none':'block';
  var hcls={Soglom:'bgn',Bogoz:'bpu',Kasal:'brd',Davolashda:'bam',Tekshiruv:'bam'};
  var tico={Novvos:'🐂',Sigir:'🐄',Bezov:'🐃',Xonajin:'🐮',Buqa:'🐂',Qocha:'🐄'};
  fl.forEach(function(m){
    var c=document.createElement('div'); c.className='mcard'; c.setAttribute('data-mol-id', m.id);
    if(m.sold){
      c.innerHTML='<div class="mctop"><div><div class="mnum">'+(tico[m.tur]||'🐄')+' #'+esc(m.raqam)+'</div>'
        +'<span class="badge bam" style="margin-top:5px;display:inline-block">💰 '+tx('sotilgan')+'</span></div>'
        +'<div style="font-size:24px">'+(tico[m.tur]||'🐄')+'</div></div>'
        +'<div class="mdet">📅 '+tx('sotilgan_sana')+': <strong>'+fd(m.sold.date)+'</strong></div>'
        +(m.sold.desc?'<div class="mdet">📝 '+esc(m.sold.desc)+'</div>':'')
        +(m.ona?'<div class="mdet">👩 Onasi: <strong>#'+esc(m.ona)+'</strong></div>':'')
        +(CU.role==='admin'?'<button class="bd" style="width:100%;margin-top:10px" data-del-mol="'+m.id+'">🗑 O\'chirish</button>':'');
      grid.appendChild(c); return;
    }
    // Bezov kelajak jinsi
    var bezovHtml = '';
    if(m.tur==='Bezov' && m.bezovJins){
      var jicon = m.bezovJins==='Novvos' ? '🐂' : '🐮';
      bezovHtml = '<div class="mdet">Kelajakda: <strong>'+jicon+' '+m.bezovJins+'</strong></div>';
    }
    // Bo'g'ozlik — joriy oyni avtomatik hisoblash
    var bogHtml = '';
    if(m.holat==='Bogoz'){
      var joriyOy = m.bogozBosh ? hozirgiBogozOy(m.bogozBosh) : (m.bogoz||null);
      var kut = m.kutilgan || (m.bogozBosh ? addMonths(m.bogozBosh, 9) : null);
      if(joriyOy){
        bogHtml = '<div class="mbog" style="display:flex;flex-direction:column;gap:4px;">'
          + '<div>🤰 Homiladorlik: <strong>'+joriyOy+'-oy</strong></div>';
        if(kut) bogHtml += '<div>📅 Kutilgan tug\'ish: <strong>'+fd(kut)+'</strong></div>';
        bogHtml += '</div>';
      }
    }
    c.innerHTML='<div class="mctop">'
      +'<div><div class="mnum">'+(tico[m.tur]||'🐄')+' #'+esc(m.raqam)+'</div>'
      +'<span class="badge '+(hcls[m.holat]||'bgn')+'" style="margin-top:5px;display:inline-block">'+(HOL_DISP[m.holat]||m.holat)+'</span></div>'
      +'<div style="font-size:24px">'+(tico[m.tur]||'🐄')+'</div></div>'
      + bezovHtml
      +(m.ona?'<div class="mdet">👩 Onasi: <strong>#'+esc(m.ona)+'</strong></div>':'')
      +(m.yosh?'<div class="mdet">Yoshi: <strong>'+esc(m.yosh)+'</strong></div>':'')
      +(m.vazn?'<div class="mdet">Og\'irligi: <strong>'+esc(m.vazn)+' kg</strong></div>':'')
      + bogHtml + milkBox(m)
      + (CU.role==='admin' ? '<div style="display:flex; gap:8px; margin-top:12px;">'
      + '<button class="btn bo" style="flex:1; font-size:12px; padding:8px 4px;" data-sold-mol="'+m.id+'">💰 '+tx('sotildi_belgilash')+'</button>'
      + '<button class="bd" style="flex:1; font-size:12px; padding:8px 4px;" data-del-mol="'+m.id+'">🗑 O\'chirish</button>'
      + '</div>' : '');
    grid.appendChild(c);
  });
}

document.getElementById('mol-grid').addEventListener('click', function(e){
  var btn=e.target.closest('[data-del-mol]');
  if(btn){ delMol(parseFloat(btn.getAttribute('data-del-mol'))); return; }
  var sb2=e.target.closest('[data-sold-mol]');
  if(sb2){ openSell(parseFloat(sb2.getAttribute('data-sold-mol'))); return; }
  var card=e.target.closest('.mcard');
  if(card){ var mid=card.getAttribute('data-mol-id'); if(mid) openMolDetail(parseFloat(mid)); }
});

var _detMolId=null;
function openMolDetail(id){
  var m=ld('FM').filter(function(x){ return x.id===id; })[0]; if(!m) return;
  _detMolId=id;
  document.getElementById('mdet-body').style.display='';
  document.getElementById('mdet-edit').style.display='';
  document.getElementById('mdet-editform').style.display='none';
  var tico={Novvos:'🐂',Sigir:'🐄',Bezov:'🐃',Xonajin:'🐮'};
  var hcls={Soglom:'bgn',Bogoz:'bpu',Kasal:'brd',Davolashda:'bam',Tekshiruv:'bam'};
  var H='';
  H+='<div class="mdhead"><div class="mdico">'+(tico[m.tur]||'🐄')+'</div><div><div class="mdname">#'+esc(m.raqam)+'</div>'
    +'<span class="badge '+(hcls[m.holat]||'bgn')+'">'+(m.sold?('💰 '+tx('sotilgan')):(HOL_DISP[m.holat]||m.holat||''))+'</span></div></div>';

  // ── Asosiy ma'lumot ──
  H+='<div class="mdsec"><div class="mdst">📋 '+tx('asosiy_malumot')+'</div>';
  H+='<div class="mdrow"><span>'+tx('tur_q')+'</span><strong>'+(tico[m.tur]||'')+' '+esc(m.tur||'')+(m.bezovJins?' ('+esc(m.bezovJins)+')':'')+'</strong></div>';
  // Tug'ilgan sana: agar Tug'ish voqeasi orqali yaratilgan bo'lsa (m.tugganMi=true) — m.q aniq tug'ilgan sana.
  // Aks holda (qo'lda qo'shilgan mol) — "Yoshi" matnidan hisoblanadi (taxminiy), ilovaga qo'shilgan sana ko'rsatilmaydi.
  if(m.tugganMi && m.q){
    H+='<div class="mdrow"><span>'+tx('tugilgan_sana')+'</span><strong>'+fd(m.q)+'</strong></div>';
  } else if(m.yosh){
    var ym=String(m.yosh).match(/(\d+(?:[.,]\d+)?)\s*(yil|oy|kun)/i);
    var n2, unit2;
    if(ym){ n2=parseFloat(ym[1].replace(',','.')); unit2=ym[2].toLowerCase(); }
    else {
      var justNum=String(m.yosh).trim().match(/^(\d+(?:[.,]\d+)?)$/);
      if(justNum){ n2=parseFloat(justNum[1].replace(',','.')); unit2='yil'; }
    }
    if(unit2){
      var days = unit2==='yil' ? Math.round(n2*365) : unit2==='oy' ? Math.round(n2*30) : Math.round(n2);
      var bd=new Date(); bd.setDate(bd.getDate()-days);
      H+='<div class="mdrow"><span>'+tx('tugilgan_sana')+' ('+tx('taxminiy')+')</span><strong>'+bd.toISOString().slice(0,10).split('-').reverse().join('.')+'</strong></div>';
    }
  }
  if(m.ona) H+='<div class="mdrow"><span>'+tx('onasi')+'</span><strong><a href="#" class="mdlink" data-go-mol="'+esc(m.ona)+'">#'+esc(m.ona)+'</a></strong></div>';
  if(m.yosh) H+='<div class="mdrow"><span>'+tx('yoshi')+'</span><strong>'+esc(m.yosh)+'</strong></div>';
  if(m.vazn) H+='<div class="mdrow"><span>'+tx('ogirligi')+'</span><strong>'+esc(m.vazn)+' kg</strong></div>';
  H+='</div>';

  // ── Sotilgan ma'lumoti ──
  if(m.sold){
    H+='<div class="mdsec"><div class="mdst">💰 '+tx('sotilgan_malumoti')+'</div>';
    H+='<div class="mdrow"><span>'+tx('sotilgan_sana')+'</span><strong>'+fd(m.sold.date)+'</strong></div>';
    if(m.sold.desc) H+='<div class="mdrow" style="display:block"><span>'+tx('tavsif')+'</span><div style="margin-top:3px"><strong>'+esc(m.sold.desc)+'</strong></div></div>';
    H+='</div>';
  }

  // ── Homiladorlik ──
  if(m.holat==='Bogoz' && m.bogozBosh){
    var joriyOy = hozirgiBogozOy(m.bogozBosh);
    var kut = m.kutilgan || addMonths(m.bogozBosh, 9);
    H+='<div class="mdsec"><div class="mdst">🤰 '+tx('homiladorlik')+'</div>';
    H+='<div class="mdrow"><span>'+tx('boshlangan_sana')+'</span><strong>'+fd(m.bogozBosh)+'</strong></div>';
    if(joriyOy) H+='<div class="mdrow"><span>'+tx('joriy_oy')+'</span><strong>'+joriyOy+'-oy</strong></div>';
    H+='<div class="mdrow"><span>'+tx('kutilgan_tugish')+'</span><strong>'+fd(kut)+'</strong></div>';
    if(joriyOy >= 7) {
      H+='<div style="display:flex; gap:8px; margin-top:12px;">'
        + '<button class="btn bo" style="flex:1; font-size:12px;" data-tugdi-mol="'+m.id+'">🐣 Tug\'di</button>'
        + '<button class="btn bs" style="flex:1; font-size:12px; background:var(--rd);" data-tugmadi-mol="'+m.id+'">❌ Tug\'madi</button>'
        + '</div>';
    }
    H+='</div>';
  }
  var tugArx = m.tugList && m.tugList.length ? m.tugList : (m.tugSana ? [{sana:m.tugSana, bolalar:[], soni:1}] : []);
  if(tugArx.length){
    H+='<div class="mdsec"><div class="mdst">🐣 '+tx('tugish_tarixi')+' ('+tugArx.length+')</div>';
    tugArx.slice().sort(function(a,b){ return String(b.sana||'').localeCompare(String(a.sana||'')); }).forEach(function(t){
      H+='<div class="mdhist"><div class="h1"><span>'+fd(t.sana)+'</span>'+(t.soni>1?'<span style="color:var(--tm);font-size:12px">'+t.soni+' '+tx('bosh_q')+'</span>':'')+'</div>';
      if(t.bolalar && t.bolalar.length){
        H+='<div class="h2">'+tx('bolalari')+': '+t.bolalar.map(function(br){ return '<a href="#" class="mdlink" data-go-mol="'+esc(br)+'">#'+esc(br)+'</a>'; }).join(', ')+'</div>';
      }
      H+='</div>';
    });
    H+='</div>';
  }

  // ── So'nggi 3 sut o'lchov ──
  if(m.tur==='Sigir'||m.tur==='Xonajin'){
    var last3=(m.sutOlchov||[]).slice().sort(function(a,b){ return String(b.date||'').localeCompare(String(a.date||'')); }).slice(0,3);
    H+='<div class="mdsec"><div class="mdst">🥛 '+tx('songgi_3_olchov')+'</div>';
    if(last3.length){
      last3.forEach(function(o){
        var e=+o.ert||0,ob=+o.obed||0,k=+o.kech||0,t=Math.round((e+ob+k)*10)/10;
        H+='<div class="mdhist"><div class="h1"><span>'+fd(o.date)+'</span><strong style="color:var(--gm)">'+t+' L</strong></div><div class="h2">🌅'+e+' ☀️'+ob+' 🌙'+k+'</div></div>';
      });
    } else H+='<div style="font-size:13px;color:var(--tm)">'+tx('olchov_yoq')+'</div>';
    H+='</div>';
  }

  // ── Sog'liq tarixi ──
  var hist=ld('FG').filter(function(r){ return r.mol===m.raqam; }).sort(function(a,b){ return String(b.date||'').localeCompare(String(a.date||'')); });
  H+='<div class="mdsec"><div class="mdst">💊 '+tx('soglik_tarixi')+' ('+hist.length+')</div>';
  if(hist.length){
    hist.slice(0,15).forEach(function(r){
      H+='<div class="mdhist"><div class="h1"><span>'+esc(SOG_DISP[r.tur]||r.tur)+'</span><span style="color:var(--tm);font-size:12px">'+fd(r.date)+'</span></div>'
        +(r.dori?'<div class="h2">💊 '+esc(r.dori)+(r.doza?' · '+esc(r.doza):'')+'</div>':'')
        +(r.tav?'<div class="h2">📝 '+esc(r.tav)+'</div>':'')
      +'</div>';
    });
    if(hist.length>15) H+='<div style="font-size:12px;color:var(--tm);text-align:center;margin-top:4px">+'+(hist.length-15)+' '+tx('yana')+'</div>';
  } else H+='<div style="font-size:13px;color:var(--tm)">'+tx('tarix_yoq')+'</div>';
  H+='</div>';

  document.getElementById('mdet-body').innerHTML=H;
  document.getElementById('mdet-mask').style.display='block';
  document.getElementById('mdet-modal').style.display='block';
}
function closeMolDetail(){ document.getElementById('mdet-mask').style.display='none'; document.getElementById('mdet-modal').style.display='none'; }
(function(){ var a=document.getElementById('mdet-close'); if(a) a.addEventListener('click', closeMolDetail); var b=document.getElementById('mdet-mask'); if(b) b.addEventListener('click', closeMolDetail);
  var body=document.getElementById('mdet-body');
  if(body) body.addEventListener('click', function(e){
    var link=e.target.closest('[data-go-mol]'); 
    if(link){
      e.preventDefault();
      var raqam=link.getAttribute('data-go-mol');
      var target=ld('FM').filter(function(x){ return String(x.raqam)===String(raqam); })[0];
      if(target) openMolDetail(target.id);
      else alert(tx('mol_topilmadi'));
      return;
    }
    
    var tLink=e.target.closest('[data-tugdi-mol]');
    if(tLink){
      e.preventDefault();
      var mid=tLink.getAttribute('data-tugdi-mol');
      var m = ld('FM').filter(function(x){ return x.id == mid; })[0];
      if(m){
        closeMolDetail();
        showPg('sog');
        var sgt = document.getElementById('sgt');
        if(sgt) { sgt.value = 'Tugish'; sgt.dispatchEvent(new Event('change')); }
        var sgm = document.getElementById('sgm');
        if(sgm) sgm.value = m.raqam;
      }
      return;
    }

    var nLink=e.target.closest('[data-tugmadi-mol]');
    if(nLink){
      e.preventDefault();
      if(!confirm("Haqiqatan ham tug'madi (homila yo'qotilgan) deb belgilamoqchimisiz?")) return;
      var mid = nLink.getAttribute('data-tugmadi-mol');
      var mols = ld('FM');
      for(var i=0; i<mols.length; i++){
        if(mols[i].id == mid){
          mols[i].holat = 'Soglom';
          mols[i].bogoz = '';
          mols[i].bogozBosh = '';
          mols[i].kutilgan = '';
          break;
        }
      }
      sv('FM', mols);
      closeMolDetail();
      if(typeof renderMolGrid === 'function') renderMolGrid();
      return;
    }
  });
})();

function openEditMol(){
  if(_detMolId===null) return;
  var m=ld('FM').filter(function(x){ return x.id===_detMolId; })[0]; if(!m) return;
  document.getElementById('med-raqam').value=m.raqam||'';
  document.getElementById('med-ona').value=m.ona||'';
  document.getElementById('med-vazn').value=m.vazn||'';
  var isBezov = m.tur==='Bezov';
  document.getElementById('med-yosh-fg').style.display = isBezov?'none':'';
  document.getElementById('med-tug-fg').style.display = isBezov?'':'none';
  if(isBezov){ document.getElementById('med-tug').value = m.tugganMi&&m.q ? m.q : ''; }
  else { document.getElementById('med-yosh').value = m.yosh||''; }
  document.getElementById('mdet-body').style.display='none';
  document.getElementById('mdet-edit').style.display='none';
  document.getElementById('mdet-editform').style.display='block';
}
function closeEditMol(){
  document.getElementById('mdet-body').style.display='';
  document.getElementById('mdet-edit').style.display='';
  document.getElementById('mdet-editform').style.display='none';
}
function saveEditMol(){
  if(_detMolId===null) return;
  var raqam=document.getElementById('med-raqam').value.trim().slice(0,20);
  if(!raqam){ alert(tx('mol_raqami')+' kiriting!'); return; }
  var mols=ld('FM'), m=null;
  for(var i=0;i<mols.length;i++){ if(mols[i].id===_detMolId){ m=mols[i]; break; } }
  if(!m) return;
  var dup=mols.filter(function(x){ return x.id!==_detMolId && String(x.raqam)===raqam; })[0];
  if(dup){ alert(tx('raqam_band')); return; }
  var oldRaqam=m.raqam;
  m.raqam=raqam;
  m.ona=document.getElementById('med-ona').value.trim().slice(0,20);
  m.vazn=document.getElementById('med-vazn').value.trim().slice(0,10);
  if(m.tur==='Bezov'){
    var tugVal=document.getElementById('med-tug').value;
    if(tugVal){ m.q=tugVal; m.tugganMi=true; }
  } else {
    m.yosh=document.getElementById('med-yosh').value.trim().slice(0,30);
  }
  // Agar raqam o'zgargan bo'lsa, shu molni bola sifatida ko'rsatgan onalarning tugList arxivini ham yangilaymiz
  if(oldRaqam!==raqam){
    mols.forEach(function(x){
      if(x.tugList) x.tugList.forEach(function(t){
        if(t.bolalar) t.bolalar=t.bolalar.map(function(b){ return b===oldRaqam?raqam:b; });
      });
      if(x.ona===oldRaqam) x.ona=raqam;
    });
  }
  sv('FM', mols);
  closeEditMol();
  openMolDetail(_detMolId);
  renderMolGrid();
  alert('✅ '+tx('saqlandi'));
}
(function(){
  var e1=document.getElementById('mdet-edit'); if(e1) e1.addEventListener('click', openEditMol);
  var e2=document.getElementById('mdet-save'); if(e2) e2.addEventListener('click', saveEditMol);
  var e3=document.getElementById('mdet-cancel'); if(e3) e3.addEventListener('click', closeEditMol);
})();

var _sellId=null;
function openSell(id){
  var m=ld('FM').filter(function(x){ return x.id===id; })[0]; if(!m) return;
  _sellId=id;
  var info=document.getElementById('sell-molinfo'); if(info) info.textContent=(m.tur||'')+' #'+(m.raqam||'');
  var dt=document.getElementById('sell-date'); if(dt) dt.value=new Date().toISOString().slice(0,10);
  var ds=document.getElementById('sell-desc'); if(ds) ds.value='';
  var mk=document.getElementById('sell-mask'), md=document.getElementById('sell-modal');
  if(mk) mk.style.display='block'; if(md) md.style.display='block';
}
function closeSell(){ _sellId=null; var mk=document.getElementById('sell-mask'), md=document.getElementById('sell-modal'); if(mk) mk.style.display='none'; if(md) md.style.display='none'; }
function saveSell(){
  if(_sellId===null) return;
  var mols=ld('FM'), m=null;
  for(var i=0;i<mols.length;i++){ if(mols[i].id===_sellId){ m=mols[i]; break; } }
  if(!m){ closeSell(); return; }
  var dt=(document.getElementById('sell-date').value)||new Date().toISOString().slice(0,10);
  var ds=(document.getElementById('sell-desc').value||'').slice(0,300);
  m.sold={ date:dt, desc:ds };
  sv('FM', mols); closeSell(); renderMolGrid(); updateDash();
}
(function(){ var a=document.getElementById('sell-save'); if(a) a.addEventListener('click', saveSell); var b=document.getElementById('sell-cancel'); if(b) b.addEventListener('click', closeSell); var m=document.getElementById('sell-mask'); if(m) m.addEventListener('click', closeSell); })();

function renderSutOlchov(){
  var dt=document.getElementById('so-date');
  if(dt && !dt.value) dt.value=new Date().toISOString().slice(0,10);
  var date=dt?dt.value:new Date().toISOString().slice(0,10);
  var allCows=ld('FM').filter(function(m){ return !m.sold && (m.tur==='Sigir'||m.tur==='Xonajin') && !m.quritilgan; });
  var cows=allCows.filter(function(m){ return !(m.sutOlchov||[]).some(function(o){ return o.date===date; }); });
  var sel=document.getElementById('so-cow'), empty=document.getElementById('so-empty'), done=document.getElementById('so-done'), inp=document.getElementById('so-input');
  if(!sel) return;
  if(!allCows.length){ if(empty) empty.style.display='block'; if(done) done.style.display='none'; sel.style.display='none'; if(inp) inp.style.display='none'; return; }
  if(!cows.length){ if(empty) empty.style.display='none'; if(done) done.style.display='block'; sel.style.display='none'; if(inp) inp.style.display='none'; return; }
  if(empty) empty.style.display='none'; if(done) done.style.display='none'; sel.style.display='';
  var tico={Sigir:'🐄',Xonajin:'🐮'};
  var cur=sel.value;
  sel.innerHTML='<option value="">— '+tx('sigirni_tanlang')+' —</option>'+cows.map(function(m){
    return '<option value="'+m.id+'">'+(tico[m.tur]||'🐄')+' #'+esc(m.raqam)+'</option>';
  }).join('');
  if(cur && cows.filter(function(m){ return String(m.id)===cur; }).length){ sel.value=cur; loadSoCow(); }
  else { sel.value=''; if(inp) inp.style.display='none'; }
}
function loadSoCow(){
  var sel=document.getElementById('so-cow'), inp=document.getElementById('so-input');
  if(!sel || !sel.value){ if(inp) inp.style.display='none'; return; }
  var id=parseFloat(sel.value);
  var m=ld('FM').filter(function(x){ return x.id===id; })[0];
  if(!m){ if(inp) inp.style.display='none'; return; }
  var date=document.getElementById('so-date').value;
  var rec=(m.sutOlchov||[]).filter(function(o){ return o.date===date; })[0] || {};
  var tico={Sigir:'🐄',Xonajin:'🐮'};
  var hd=document.getElementById('so-cowhead'); if(hd) hd.textContent=(tico[m.tur]||'🐄')+' #'+(m.raqam||'');
  var warn=document.getElementById('so-kolostrum');
  if(warn){
    if(m.tugSana){
      var kunlar=Math.round((Date.parse(date)-Date.parse(m.tugSana))/86400000);
      if(kunlar>=0 && kunlar<10){ warn.style.display='block'; warn.textContent='⚠️ '+tx('kolostrum_ogoh')+' ('+kunlar+'/10 kun)'; }
      else warn.style.display='none';
    } else warn.style.display='none';
  }
  document.getElementById('so-ert').value=(rec.ert!=null?rec.ert:'');
  document.getElementById('so-obed').value=(rec.obed!=null?rec.obed:'');
  document.getElementById('so-kech').value=(rec.kech!=null?rec.kech:'');
  updateSoTotal();
  if(inp) inp.style.display='';
}
function updateSoTotal(){
  var e=parseFloat(document.getElementById('so-ert').value)||0, o=parseFloat(document.getElementById('so-obed').value)||0, k=parseFloat(document.getElementById('so-kech').value)||0;
  var t=document.getElementById('so-tot'); if(t) t.textContent=(Math.round((e+o+k)*10)/10);
}
function saveSutOlchov(){
  var sel=document.getElementById('so-cow'); if(!sel || !sel.value){ alert(tx('sigir_tanla')); return; }
  var id=parseFloat(sel.value);
  var date=document.getElementById('so-date').value || new Date().toISOString().slice(0,10);
  var mols=ld('FM'), m=null;
  for(var i=0;i<mols.length;i++){ if(mols[i].id===id){ m=mols[i]; break; } }
  if(!m) return;
  var ev=document.getElementById('so-ert').value, ov=document.getElementById('so-obed').value, kv=document.getElementById('so-kech').value;
  if(ev===''&&ov===''&&kv===''){ alert(tx('sut_kiriting')); return; }
  if(!m.sutOlchov) m.sutOlchov=[];
  m.sutOlchov=m.sutOlchov.filter(function(o){ return o.date!==date; });
  m.sutOlchov.push({ date:date, ert:parseFloat(ev)||0, obed:parseFloat(ov)||0, kech:parseFloat(kv)||0 });
  sv('FM', mols); renderMolGrid();
  alert(tx('so_saqlandi'));
  renderSutOlchov();
}
(function(){
  var sc=document.getElementById('so-cow'); if(sc) sc.addEventListener('change', loadSoCow);
  var sd=document.getElementById('so-date'); if(sd) sd.addEventListener('change', renderSutOlchov);
  ['so-ert','so-obed','so-kech'].forEach(function(id){ var el=document.getElementById(id); if(el) el.addEventListener('input', updateSoTotal); });
  var ss=document.getElementById('so-save'); if(ss) ss.addEventListener('click', saveSutOlchov);
})();

// ── SOG'LIQ ──
document.getElementById('sgt').addEventListener('change', function(){
  var v=this.value;
  var isTug = v==='Tugish';
  var isUrug = v==='Urugqoyish';
  var isEml = v==='Emlash';
  document.getElementById('tug-f').style.display = isTug?'block':'none';
  var sgw2=document.getElementById('sgtt2-wrap'); if(sgw2) sgw2.style.display=(isTug && document.getElementById('sgts').value==='2')?'':'none';
  document.getElementById('urug-f').style.display = isUrug?'block':'none';
  // Dori/vaksina va doza — tug'ish, va urug' qo'yishda kerak emas
  document.getElementById('doza-row').style.display = (isTug||isUrug)?'none':'';
  document.getElementById('dori-fg').style.display = (isTug||isUrug)?'none':'';
  // Emlash: guruh-rejim (mol raqami o'rniga guruh tanlash)
  document.getElementById('molraqam-fg').style.display = isEml?'none':'';
  document.getElementById('emlash-guruh').style.display = isEml?'block':'none';
  if(isEml){ curEmGuruh='all'; document.querySelectorAll('#emlash-guruh .chip').forEach(function(c){ c.classList.toggle('on', c.getAttribute('data-eg')==='all'); }); updateEmlashSoni(); }
});

var curEmGuruh = 'all';
function emGuruhMollar(g){
  var active=ld('FM').filter(function(m){ return !m.sold; });
  if(g==='all') return active;
  if(g==='Bogoz') return active.filter(function(m){ return m.holat==='Bogoz'; });
  return active.filter(function(m){ return m.tur===g; });
}
function updateEmlashSoni(){
  var n=emGuruhMollar(curEmGuruh).length;
  var el=document.getElementById('emlash-soni');
  if(el) el.textContent='📋 '+n+' '+tx('bosh_q')+' ('+tx(curEmGuruh==='all'?'eg_hammasi':curEmGuruh==='Bogoz'?'eg_bogozlar':curEmGuruh==='Sigir'?'eg_sigirlar':curEmGuruh==='Novvos'?'eg_novvoslar':curEmGuruh==='Bezov'?'eg_bezovlar':'eg_xonajinlar')+')';
}
document.getElementById('emlash-guruh').addEventListener('click', function(e){
  var c=e.target.closest('.chip'); if(!c) return;
  curEmGuruh=c.getAttribute('data-eg');
  document.querySelectorAll('#emlash-guruh .chip').forEach(function(x){ x.classList.toggle('on', x===c); });
  updateEmlashSoni();
});


document.getElementById('add-sog-btn').addEventListener('click', addSog);

document.getElementById('sog-tabs').addEventListener('click', function(e){
  var btn=e.target.closest('.tab');
  if(!btn) return;
  document.querySelectorAll('#sog-tabs .tab').forEach(function(b){ b.classList.remove('on'); });
  btn.classList.add('on');
  renderSog(btn.getAttribute('data-sf'));
});

function addSog(){
  var date=document.getElementById('sgd').value;
  var tur=document.getElementById('sgt').value;
  if(tur==='Emlash'){
    if(!date){ alert(tx('sana')+' kiriting!'); return; }
    var dori2=document.getElementById('sgdr').value.trim().slice(0,80);
    var doza2=document.getElementById('sgdz').value.trim().slice(0,40);
    var key2=document.getElementById('sgky').value;
    var tav2=document.getElementById('sgtav').value.trim().slice(0,500);
    var targets=emGuruhMollar(curEmGuruh);
    if(!targets.length){ alert(tx('eg_bosh')); return; }
    var recsG=ld('FG');
    targets.forEach(function(m){
      recsG.push({id:Date.now()+Math.random(),date:date,tur:'Emlash',mol:m.raqam,dori:dori2,doza:doza2,key:key2,tav:tav2,w:CU.name});
    });
    sv('FG',recsG);
    ['sgdr','sgdz','sgky','sgtav'].forEach(function(id){ var e=document.getElementById(id);if(e)e.value=''; });
    renderSog(); refreshNotifs();
    alert('✅ '+targets.length+' '+tx('bosh_q')+' — '+tx('emlandi_msg'));
    return;
  }
  var mol=document.getElementById('sgm').value.trim();
  if(!date||!mol){ alert(tx('sana')+' va mol raqamini kiriting!'); return; }
  var extra={};
  if(tur==='Tugish'){
    extra.tSon=document.getElementById('sgts').value;
    extra.tVazn=document.getElementById('sgtv').value;
    extra.tRaq=document.getElementById('sgtr').value.trim().slice(0,20);
    // Har bir tug'ilgan -> Bezov, o'z jinsi (Novvos/Xonajin) bilan avtomatik qo'shiladi
    var cnt = parseInt(extra.tSon)||1;
    var jins1=document.getElementById('sgtt').value;
    var j2el=document.getElementById('sgtt2'); var jins2=j2el?j2el.value:jins1;
    var jinsList=[jins1]; if(cnt>=2) jinsList.push(jins2);
    extra.tTur='Bezov'; extra.tJins=jinsList.join(', ');
    var mols2 = ld('FM');
    function nextMolNum(){ var mx=0; mols2.forEach(function(m){ var n=parseInt(String(m.raqam||'').replace(/\D/g,'')); if(!isNaN(n)&&n>mx) mx=n; }); return mx+1; }
    var baseN = parseInt(String(extra.tRaq||'').replace(/\D/g,''));
    var createdNums=[];
    var r2el = document.getElementById('sgtr2');
    var rq2 = (r2el && r2el.value.trim()) ? r2el.value.trim().slice(0,20) : '';
    for(var bi=0; bi<cnt; bi++){
      var rq;
      if (bi === 0) {
        rq = extra.tRaq || String(nextMolNum());
      } else {
        rq = rq2 || ((!isNaN(baseN)) ? String(baseN+bi) : String(nextMolNum()));
      }
      createdNums.push(rq);
      mols2.push({ id:Date.now()+bi+1, raqam:rq, tur:'Bezov', yosh:'', vazn:extra.tVazn||'', holat:'Soglom', bogoz:'', ona:(mol||'').replace('#',''), q:date, tugganMi:true, bezovJins:(jinsList[bi]||jinsList[0]) });
    }
    // Onaning holati: tug'di -> endi bo'g'oz emas, tugSana saqlanadi (kolostrum hisobi uchun), tugList arxivga qo'shiladi
    for(var mi=0; mi<mols2.length; mi++){
      if(mols2[mi].raqam===mol || mols2[mi].raqam===mol.replace('#','')){
        mols2[mi].tugSana=date;
        if(!mols2[mi].tugList) mols2[mi].tugList=[];
        mols2[mi].tugList.push({ sana:date, bolalar:createdNums.slice(), soni:cnt });
        if(mols2[mi].holat==='Bogoz'){ mols2[mi].holat='Soglom'; mols2[mi].bogoz=''; mols2[mi].bogozBosh=''; mols2[mi].kutilgan=''; mols2[mi].quritilgan=''; }
        break;
      }
    }
    sv('FM', mols2);
    extra.tRaqlar = createdNums.join(', ');
  }
  if(tur==='Urugqoyish'){
    var molsU=ld('FM'), targetU=null;
    for(var ui=0;ui<molsU.length;ui++){ if(molsU[ui].raqam===mol||molsU[ui].raqam===mol.replace('#','')){ targetU=molsU[ui]; break; } }
    if(!targetU){ alert(tx('mol_topilmadi')); return; }
    if(targetU.tur!=='Sigir' && targetU.tur!=='Xonajin'){ alert(tx('faqat_sigir_bogoz')); return; }
    if(targetU.holat==='Bogoz'){ alert("Bu mol allaqachon bo'g'oz (homilador)!"); return; }
    targetU.holat='Tekshiruv';
    targetU.urugSana=date;
    targetU.tekshirSana=addDays(date,45);
    targetU.urugTimestamp=Date.now();
    sv('FM',molsU);
    extra.uSana=date;
  }

  if(tur==='Davolash'){
    var molsD=ld('FM');
    for(var di=0;di<molsD.length;di++){ if(molsD[di].raqam===mol||molsD[di].raqam===mol.replace('#','')){ molsD[di].holat='Davolashda'; break; } }
    sv('FM',molsD);
  }
  var dori=document.getElementById('sgdr').value.trim().slice(0,80);
  var doza=tur==='Tugish'?'':document.getElementById('sgdz').value.trim().slice(0,40);
  var key=document.getElementById('sgky').value;
  var tav=document.getElementById('sgtav').value.trim().slice(0,500);
  var recs=ld('FG'); recs.push(Object.assign({id:Date.now(),date:date,tur:tur,mol:mol,dori:dori,doza:doza,key:key,tav:tav,w:CU.name},extra));
  sv('FG',recs);
  ['sgm','sgdr','sgdz','sgky','sgtav','sgtv','sgtr','sgtr2','sgbo','sgku'].forEach(function(id){ var e=document.getElementById(id);if(e)e.value=''; });
  var sgtsR=document.getElementById('sgts'); if(sgtsR) sgtsR.value='1';
  var sgw2R=document.getElementById('sgtt2-wrap'); if(sgw2R) sgw2R.style.display='none';
  var sgr2R=document.getElementById('sgtr2-wrap'); if(sgr2R) sgr2R.style.display='none';
  document.getElementById('tug-f').style.display='none';
  document.getElementById('bog-f').style.display='none';
  document.getElementById('urug-f').style.display='none';
  renderSog(); renderMolGrid(); refreshNotifs();
  showTmpAlert('pg-sog');
}

function delSog(id){
  sv('FG',ld('FG').filter(function(r){ return r.id!==id; }));
  renderSog();
}

function recoveredSog(id){
  var recs=ld('FG'); var rec=null;
  for(var i=0;i<recs.length;i++){ if(recs[i].id===id){ rec=recs[i]; recs[i].recovered=new Date().toISOString().slice(0,10); break; } }
  if(!rec) return;
  var molNo=String(rec.mol||'').replace('#','');
  var mols=ld('FM');
  for(var j=0;j<mols.length;j++){ if(mols[j].raqam===molNo || mols[j].raqam===rec.mol){ mols[j].holat='Soglom'; break; } }
  sv('FG',recs); sv('FM',mols);
  renderSog(); renderMolGrid(); updateDash(); refreshNotifs();
  alert('✅ '+tx('mol')+' №'+rec.mol+' '+tx('davolanishdan_chiqdi_msg'));
}

var curSogFilter='all';
function renderSog(filter){
  if(filter!==undefined) curSogFilter=filter;
  var recs=ld('FG');
  if(curSogFilter!=='all') recs=recs.filter(function(r){ return r.tur===curSogFilter; });
  recs.sort(function(a,b){ return b.id-a.id; });
  var list=document.getElementById('sog-list');
  if(!recs.length){ list.innerHTML='<div class="empty"><div class="ei">💉</div><div class="et">'+tx('yoq')+'</div></div>'; return; }
  var tc={Emlash:'bgn',Davolash:'brd',Tugish:'bam',VetKorik:'bbl',Bogozlik:'bpu',Boshqa:'bgr'};
  list.innerHTML=recs.map(function(r){
    return '<div class="hentry">'
      +'<div class="htop"><div><div class="hmol">🐄 '+esc(r.mol)+'</div><div class="hd">'+fd(r.date)+' · '+(esc(r.w)||'—')+'</div></div>'
      +'<div style="display:flex;align-items:center;gap:6px;flex-shrink:0">'
      +'<span class="badge '+(tc[r.tur]||'bbl')+'">'+esc(SOG_DISP[r.tur]||r.tur)+'</span>'
      +(CU.role==='admin'?'<button class="bd" data-del-sog="'+r.id+'">🗑</button>':'')
      +'</div></div>'
      +'<div class="htags">'
      +(r.dori?'<span class="badge bbl">💊 '+esc(r.dori)+'</span>':'')
      +(r.doza?'<span class="badge bam">Doza: '+esc(r.doza)+'</span>':'')
      +(r.key?'<span class="badge bpu">📅 '+fd(r.key)+'</span>':'')
      +(r.tJins?'<span class="badge bam">🐣 '+esc(r.tJins)+'</span>':(r.tTur?'<span class="badge bam">🐮 '+esc(r.tTur)+'</span>':''))
      +(r.tVazn?'<span class="badge bgr">'+r.tVazn+'kg</span>':'')
      +(r.tRaq?'<span class="badge bgn">#'+esc(r.tRaq)+'</span>':'')
      +(r.bOy?'<span class="badge bpu">🤰 '+r.bOy+'-oy</span>':'')
      +'</div>'
      +(r.tav?'<div style="margin-top:6px;font-size:12px">'+esc(r.tav)+'</div>':'')
      +(r.tur==='Davolash' ? (r.recovered
          ? '<div style="margin-top:8px"><span class="badge bgn">✅ '+tx('soglom_boldi')+' · '+fd(r.recovered)+'</span></div>'
          : ((CU.role==='admin'||CU.role==='doktor') ? '<button class="bd" style="background:#16a34a;color:#fff;width:100%;margin-top:10px;font-weight:700" data-recover-sog="'+r.id+'">✅ '+tx('davolanishdan_chiqdi')+'</button>' : ''))
        : '')
      +'</div>';
  }).join('');
}

document.getElementById('sog-list').addEventListener('click', function(e){
  var rb=e.target.closest('[data-recover-sog]');
  if(rb){ recoveredSog(parseFloat(rb.getAttribute('data-recover-sog'))); return; }
  var btn=e.target.closest('[data-del-sog]');
  if(btn) delSog(parseFloat(btn.getAttribute('data-del-sog')));
});

// ── YEM KELISH ──
document.getElementById('add-kel-btn').addEventListener('click', addKel);

// Umumiy summani avtomatik hisoblash (narx × miqdor)
function updateKelSumma(){
  var miq=parseFloat(document.getElementById('kmiq').value)||0;
  var narx=parseFloat(document.getElementById('knarx').value)||0;
  var br=document.getElementById('kbr').value;
  var box=document.getElementById('kel-summa');
  if(miq>0 && narx>0){
    var jami = miq*narx;
    document.getElementById('kel-summa-val').textContent = jami.toLocaleString('uz-UZ')+" so'm";
    document.getElementById('kel-summa-hesob').textContent = miq+' '+br+' × '+narx.toLocaleString('uz-UZ')+" so'm";
    box.style.display='';
  } else {
    box.style.display='none';
  }
}
document.getElementById('kmiq').addEventListener('input', updateKelSumma);
document.getElementById('knarx').addEventListener('input', updateKelSumma);
document.getElementById('kbr').addEventListener('change', updateKelSumma);

function addKel(){
  var date=document.getElementById('kd').value;
  var tur=document.getElementById('ktur').value;
  var miq=num('kmiq',0,1000000);
  var br=document.getElementById('kbr').value;
  var narx=num('knarx',0,100000000);
  var kim=document.getElementById('kkim').value.trim().slice(0,40);
  if(!date||isNaN(miq)||miq<=0){ alert(tx('sana')+' va miqdorni kiriting!'); return; }
  // narx = 1 birlik narxi, jamiNarx = narx × miqdor
  var jamiNarx = narx * miq;
  // kg ga normallashtirilgan miqdor (tonna bo'lsa ×1000)
  var miqKg = br==='tonna' ? miq*1000 : miq;
  var recs=ld('FK'); recs.push({id:Date.now(),date:date,tur:tur,miq:miq,br:br,miqKg:miqKg,narx:narx,jamiNarx:jamiNarx,kim:kim,w:CU.name});
  sv('FK',recs);
  ['kmiq','knarx','kkim'].forEach(function(id){ document.getElementById(id).value=''; });
  document.getElementById('kel-summa').style.display='none';
  renderKel(); renderOmbor();
  showTmpAlert('pg-kel');
}

function delKel(id){
  sv('FK',ld('FK').filter(function(r){ return r.id!==id; }));
  renderKel(); renderOmbor();
}

function renderKel(){
  var mo=document.getElementById('kel-fm').value;
  var recs=ld('FK');
  if(mo) recs=recs.filter(function(r){ return r.date.indexOf(mo)===0; });
  recs.sort(function(a,b){ return b.date<a.date?-1:b.date>a.date?1:b.id-a.id; });
  var list=document.getElementById('kel-list');
  if(!recs.length){ list.innerHTML='<div class="empty"><div class="ei">📦</div><div class="et">'+tx('yoq')+'</div></div>'; return; }
  list.innerHTML=recs.map(function(r){
    return '<div class="kelbox">'
      +'<div class="keltop"><div><div class="keln">📦 '+esc(fName(r.tur))+'</div><div class="keld">'+fd(r.date)+(r.kim?' · '+esc(r.kim):'')+'</div></div>'
      +'<div style="display:flex;align-items:center;gap:7px">'
      +'<span style="font-size:15px;font-weight:800;color:var(--gm)">'+r.miq+' '+uName(r.br)+'</span>'
      +(CU.role==='admin'?'<button class="bd" data-del-kel="'+r.id+'">🗑</button>':'')
      +'</div></div>'
      +'<div class="keltags">'
      +(r.narx?'<span class="badge bgn">💵 '+r.narx.toLocaleString()+"/"+uName(r.br)+"</span>":'')
      +(r.jamiNarx?'<span class="badge bbl">💰 '+r.jamiNarx.toLocaleString()+" so'm</span>":'')
      +'<span class="badge bgr">👤 '+(esc(r.w)||'—')+'</span>'
      +'</div></div>';
  }).join('');
}

document.getElementById('kel-list').addEventListener('click', function(e){
  var btn=e.target.closest('[data-del-kel]');
  if(btn) delKel(parseFloat(btn.getAttribute('data-del-kel')));
});

// ── OMBOR ──
function renderOmbor(){
  var mo=document.getElementById('ombor-m').value;
  var kelRecs=ld('FK'); if(mo) kelRecs=kelRecs.filter(function(r){ return r.date.indexOf(mo)===0; });
  var yemRecs=ld('FY'); if(mo) yemRecs=yemRecs.filter(function(r){ return r.date.indexOf(mo)===0; });
  var kelMap={}, sarfMap={};
  kelRecs.forEach(function(r){ if(!kelMap[r.tur]) kelMap[r.tur]={miq:0,br:''}; kelMap[r.tur].miq+=r.miq; kelMap[r.tur].br=r.br; });
  yemRecs.forEach(function(r){ if(!sarfMap[r.tur]) sarfMap[r.tur]={jami:0,u:''}; sarfMap[r.tur].jami+=r.jami; sarfMap[r.tur].u=r.u||'kg'; });
  var allTypes={};
  Object.keys(kelMap).forEach(function(k){ allTypes[k]=true; });
  Object.keys(sarfMap).forEach(function(k){ allTypes[k]=true; });
  var types=Object.keys(allTypes);
  var tb=document.getElementById('ombor-tb'); tb.innerHTML='';
  var empty=document.getElementById('ombor-empty');
  if(!types.length){ empty.style.display='block'; return; }
  empty.style.display='none';
  var totKel=0,totSarf=0,kamCount=0;
  types.forEach(function(tur){
    var kel=kelMap[tur]?kelMap[tur].miq:0;
    var sarf=sarfMap[tur]?sarfMap[tur].jami:0;
    var br=(kelMap[tur]&&kelMap[tur].br)||(sarfMap[tur]&&sarfMap[tur].u)||'kg';
    var qol=kel-sarf;
    var holat=qol<0?'⚠️ Kam':qol===0?'🟡 Tugadi':'✅ Bor';
    var qcls=qol<0?'cr fw7':qol===0?'ca fw7':'cg fw7';
    if(br!=='litr'){ totKel+=kel; totSarf+=sarf; }
    if(qol<0) kamCount++;
    var tr=document.createElement('tr');
    tr.innerHTML='<td><strong>'+esc(fName(tur))+'</strong></td>'
      +'<td>'+(kel?kel+' '+uName(br):'—')+'</td>'
      +'<td>'+(sarf?sarf+' '+uName(br):'—')+'</td>'
      +'<td class="'+qcls+'">'+qol+' '+uName(br)+'</td>'
      +'<td>'+holat+'</td>';
    tb.appendChild(tr);
  });
  document.getElementById('ombor-sg').innerHTML=
    '<div class="sc gn"><div class="si">📦</div><div class="sv">'+types.length+'</div><div class="sl">'+tx('tur_soni')+'</div></div>'
    +'<div class="sc am"><div class="si">⬇️</div><div class="sv">'+totKel+'kg</div><div class="sl">'+tx('jami_keldi')+'</div></div>'
    +'<div class="sc bl"><div class="si">⬆️</div><div class="sv">'+totSarf+'kg</div><div class="sl">'+tx('jami_sarf')+'</div></div>'
    +'<div class="sc '+(kamCount>0?'rd':'gn')+'"><div class="si">'+(kamCount>0?'⚠️':'✅')+'</div><div class="sv">'+kamCount+'</div><div class="sl">'+tx('kamaygan')+'</div></div>';
}

// ── DASHBOARD ──
function updateDash(){
  var now=new Date(); var td=now.toISOString().slice(0,10); var mo=td.slice(0,7);
  var sr=ld('FS'), yr=ld('FY'), ml=ld('FM');
  var st=0,bt=0,sm=0,yt=0,ym=0,todayMs=0;
  sr.forEach(function(r){ if(r.date===td){st+=r.litr;bt+=(r.buz||0);todayMs+=(r.ms||0);} if(r.date.indexOf(mo)===0) sm+=r.litr; });
  yr.forEach(function(r){ if(r.date===td) yt+=r.jami; if(r.date.indexOf(mo)===0) ym+=r.jami; });
  var days={}; sr.filter(function(r){ return r.date.indexOf(mo)===0; }).forEach(function(r){ days[r.date]=1; });
  var dc=Object.keys(days).length;
  var ortKun=dc>0?Math.round(sm/dc):0;
  var ortMol=todayMs>0?(st/todayMs).toFixed(1):0;
  var cows=ml.filter(function(m){ return !m.sold && (m.tur==='Sigir'||m.tur==='Xonajin'); }).length;
  if(cows<=0) cows=todayMs;
  var ortMolOy=cows>0?Math.round(sm/cows):0;
  document.getElementById('s1').textContent=st+'L';
  document.getElementById('s3').textContent=yt+'kg';
  document.getElementById('s5').textContent=sm+'L';
  document.getElementById('s6').textContent=ym+'kg';
  document.getElementById('s7').textContent=ortMol+'L';
  var s9e=document.getElementById('s9'); if(s9e) s9e.textContent=ortMolOy+'L';
  document.getElementById('s8').textContent=ortKun+'L';
  var rs=[].concat(sr).sort(function(a,b){ return b.id-a.id; }).slice(0,5);
  document.getElementById('rsut').innerHTML=rs.length?rs.map(function(r){
    return '<div class="ri"><div class="rico m">🥛</div>'
      +'<div class="rin"><div class="rit">'+fd(r.date)+(r.ms?' · '+r.ms+' mol':'')+'</div><div class="rim">'+esc(r.w||'')+'</div></div>'
      +'<div class="riv">'+r.litr+'L</div></div>';
  }).join(''):'<div class="empty"><div class="ei">🥛</div><div class="et">'+tx('yoq')+'</div></div>';
  // Bugungi yem taqsimoti: jami + guruh bo'yicha + 1 boshga o'rtacha (kg)
  function yemKg(r){ var u=(r.u||'kg'); if(u==='dona') return r.jami*1.5; return r.jami; } // litr ham kg (1:1)
  var yemDay = td;
  var dayRecs = yr.filter(function(r){ return r.date===td; });
  if(!dayRecs.length && yr.length){
    var allD = yr.map(function(r){ return r.date; }).sort();
    yemDay = allD[allD.length-1];
    dayRecs = yr.filter(function(r){ return r.date===yemDay; });
  }
  function grpKg(g){ var s=0; dayRecs.forEach(function(r){ if(r.guruh===g) s+=yemKg(r); }); return Math.round(s); }
  function grpMs(g){ var m=0; dayRecs.forEach(function(r){ if(r.guruh===g && r.ms) m=r.ms; }); return m; }
  var sigKg=grpKg('Sigirlar'), xonKg=grpKg('Xonajinlar'), buzKg=grpKg('Buzoqlar');
  var jamiKg=sigKg+xonKg+buzKg;
  var sigH=grpMs('Sigirlar')||ml.filter(function(m){return m.tur==='Sigir';}).length;
  var xonH=grpMs('Xonajinlar')||ml.filter(function(m){return m.tur==='Xonajin';}).length;
  var sigPer=sigH>0?Math.round((sigKg/sigH)*10)/10:0;
  var xonPer=xonH>0?Math.round((xonKg/xonH)*10)/10:0;
  var phBox=document.getElementById('dash-perhead'), phCard=document.getElementById('dash-perhead-card');
  if(phBox){
    if(dayRecs.length){
      var dayNote=(yemDay===td)?tx('bugun_q'):fd(yemDay);
      var H='<div class="dyrow" style="font-size:15px"><span><strong>\ud83c\udf3e '+tx('jami_yem')+'</strong></span><span><strong class="ca" style="font-size:18px">'+jamiKg+' '+uName('kg')+'</strong></span></div>';
      if(sigKg>0){
        H+='<div class="dyg">\ud83d\udc04 '+tx('guruh_sigirlar')+'</div>';
        H+='<div class="dyrow"><span>'+tx('jami_berilgan')+'</span><span><strong>'+sigKg+' '+uName('kg')+'</strong></span></div>';
        H+='<div class="dyrow"><span>'+tx('bir_boshga')+(sigH>0?' ('+sigH+' '+tx('bosh_q')+')':'')+'</span><span>'+(sigH>0?'<strong class="ca">'+sigPer+' '+uName('kg')+'/'+tx('bosh_q')+'</strong>':'<span style="color:#b45309;font-size:12px">'+tx('bosh_kiritilmagan')+'</span>')+'</span></div>';
      }
      if(xonKg>0){
        H+='<div class="dyg">\ud83d\udc02 '+tx('guruh_xonajinlar')+'</div>';
        H+='<div class="dyrow"><span>'+tx('jami_berilgan')+'</span><span><strong>'+xonKg+' '+uName('kg')+'</strong></span></div>';
        H+='<div class="dyrow"><span>'+tx('bir_boshga')+(xonH>0?' ('+xonH+' '+tx('bosh_q')+')':'')+'</span><span>'+(xonH>0?'<strong class="ca">'+xonPer+' '+uName('kg')+'/'+tx('bosh_q')+'</strong>':'<span style="color:#b45309;font-size:12px">'+tx('bosh_kiritilmagan')+'</span>')+'</span></div>';
      }
      if(buzKg>0){
        H+='<div class="dyg">\ud83d\udc2e '+tx('guruh_buzoqlar')+'</div>';
        H+='<div class="dyrow"><span>'+tx('jami_berilgan')+'</span><span><strong>'+buzKg+' '+uName('kg')+'</strong></span></div>';
      }
      H+='<div style="font-size:11px;color:var(--tm);margin-top:8px">'+tx('yem_note')+' \u00b7 '+dayNote+'</div>';
      phBox.innerHTML=H;
      if(phCard) phCard.style.display='';
    } else { if(phCard) phCard.style.display='none'; }
  }
  renderDashAlerts();
  refreshNotifs();
}

// ── DASHBOARD OGOHLANTIRISHLAR ──
function renderDashAlerts(){
  var box = document.getElementById('dash-alerts');
  if(!box) return;
  var html = '';
  var bugun = new Date().toISOString().slice(0,10);

  // 1) Emlash/ko'rik eslatmalari — keyingi ko'rik sanasi bugun yoki o'tib ketgan
  var sogRecs = ld('FG');
  var emlashSet = {}; // bir molni bir marta ko'rsatish
  sogRecs.forEach(function(r){
    if(r.key && r.key <= bugun && r.tur!=='Tugish'){
      var kun = r.key;
      var kalit = r.mol+'|'+kun;
      if(!emlashSet[kalit]){
        emlashSet[kalit] = true;
        var otib = r.key < bugun;
        html += '<div class="alert-red"><span style="font-size:17px">⚠️</span><span>'
          + tx('emlash_alert1') + ' №' + r.mol + tx('emlash_alert2')
          + (otib ? ' <span style="opacity:.8">('+fd(r.key)+')</span>' : '')
          + '</span></div>';
      }
    }
  });

  // 2) Ombor zaxirasi kam — joriy oyda qoldiq 500 kg dan kam
  var mo = bugun.slice(0,7);
  var kelRecs = ld('FK').filter(function(r){ return r.date.indexOf(mo)===0; });
  var yemRecs = ld('FY').filter(function(r){ return r.date.indexOf(mo)===0; });
  var kelMap={}, sarfMap={}, brMap={};
  kelRecs.forEach(function(r){ kelMap[r.tur]=(kelMap[r.tur]||0)+r.miq; brMap[r.tur]=r.br; });
  yemRecs.forEach(function(r){ sarfMap[r.tur]=(sarfMap[r.tur]||0)+r.jami; if(!brMap[r.tur])brMap[r.tur]=r.u||'kg'; });
  var allT={}; Object.keys(kelMap).forEach(function(k){allT[k]=1;}); Object.keys(sarfMap).forEach(function(k){allT[k]=1;});
  Object.keys(allT).forEach(function(tur){
    var br = brMap[tur]||'kg';
    if(br!=='kg') return; // faqat kg o'lchovли yemlar uchun
    var qol = (kelMap[tur]||0) - (sarfMap[tur]||0);
    if(qol < 500 && (kelMap[tur]||0) > 0){
      html += '<div class="alert-orange"><span style="font-size:17px">📦</span><span>'
        + tx('ombor_alert1') + ' ' + tur + ' ' + tx('ombor_alert2')
        + ' (' + qol + ' kg)!</span></div>';
    }
  });

  box.innerHTML = html;
}

// ── DAROMAD ──
function renderDar(){
  var mo=document.getElementById('dar-m').value;
  var narx=parseFloat(document.getElementById('narx').value)||0;
  var rows=ld('FS'); if(mo) rows=rows.filter(function(r){ return r.date.indexOf(mo)===0; });
  var tS=0,tK=0,tT=0;
  rows.forEach(function(r){ tS+=r.litr; tK+=(r.komp||0); tT+=(r.tash||0); });
  var jami=(tK+tT)*narx;
  document.getElementById('dar-sg').innerHTML=
    '<div class="sc gn"><div class="si">🥛</div><div class="sv">'+tS+'L</div><div class="sl">Jami sut</div></div>'
    +'<div class="sc bl"><div class="si">🏭</div><div class="sv">'+tK+'L</div><div class="sl">Kompaniya</div></div>'
    +'<div class="sc am"><div class="si">🛒</div><div class="sv">'+tT+'L</div><div class="sl">Tashqariga</div></div>'
    +'<div class="sc pu"><div class="si">💵</div><div class="sv">'+fmtS(jami)+'</div><div class="sl">Daromad</div></div>';
  var bD={};
  rows.forEach(function(r){ if(!bD[r.date]) bD[r.date]={l:0,k:0,t:0}; bD[r.date].l+=r.litr; bD[r.date].k+=(r.komp||0); bD[r.date].t+=(r.tash||0); });
  var days=Object.keys(bD).sort().reverse();
  document.getElementById('dar-det').innerHTML=days.length?
    '<div class="tw"><table><thead><tr><th>Sana</th><th>Sut</th><th>Komp.</th><th>Tash.</th><th>Daromad</th></tr></thead><tbody>'
    +days.map(function(d){ return '<tr><td><strong>'+fd(d)+'</strong></td><td>'+bD[d].l+'L</td><td>'+bD[d].k+'L</td><td>'+bD[d].t+'L</td><td><strong class="cg">'+fmtS((bD[d].k+bD[d].t)*narx)+'</strong></td></tr>'; }).join('')
    +'</tbody></table></div>'
    :'<div class="empty"><div class="ei">💰</div><div class="et">'+tx('yoq')+'</div></div>';
  renderROI();
}

// ── ROI / TANNARX KALKULYATORI ──
function renderROI(){
  var mo = document.getElementById('dar-m').value;
  var sotuvNarx = parseFloat(document.getElementById('narx').value)||0;

  // 1) Yalpi daromad = joriy oy umumiy sut × sotuv narxi
  var sutRecs = ld('FS'); if(mo) sutRecs = sutRecs.filter(function(r){ return r.date.indexOf(mo)===0; });
  var jamiSut = 0;
  sutRecs.forEach(function(r){ jamiSut += r.litr; });
  var yalpiDaromad = jamiSut * sotuvNarx;

  // 2) Yem xarajati = sarflangan yem × 1 kg/birlik narxi (Yem kelish narxlaridan)
  // Birlik narx = jami summa / jami miqdor(kg). tonna→kg avtomatik hisoblangan.
  var kelAll = ld('FK');
  var birlikNarx = {};   // har yem turi uchun 1 kg/dona narxi
  var kelAgg = {};
  kelAll.forEach(function(r){
    if(!kelAgg[r.tur]) kelAgg[r.tur] = {summa:0, miqKg:0};
    // eski yozuvlarda jamiNarx/miqKg bo'lmasligi mumkin — moslashtiramiz
    var summa = (r.jamiNarx!=null) ? r.jamiNarx : ((r.narx||0) * (r.miq||0));
    var mkg = (r.miqKg!=null) ? r.miqKg : (r.br==='tonna' ? (r.miq||0)*1000 : (r.miq||0));
    kelAgg[r.tur].summa += summa;
    kelAgg[r.tur].miqKg += mkg;
  });
  Object.keys(kelAgg).forEach(function(tur){
    var k = kelAgg[tur];
    birlikNarx[tur] = k.miqKg>0 ? k.summa/k.miqKg : 0;
  });

  var yemRecs = ld('FY'); if(mo) yemRecs = yemRecs.filter(function(r){ return r.date.indexOf(mo)===0; });
  var yemXarajat = 0;
  yemRecs.forEach(function(r){
    var bn = birlikNarx[r.tur] || 0;
    yemXarajat += (r.jami||0) * bn;  // r.jami kg yoki dona — birlik narx bilan mos
  });
  yemXarajat = Math.round(yemXarajat);

  // 3) Sof foyda va 1L tannarx
  var sofFoyda = yalpiDaromad - yemXarajat;
  var tannarx = jamiSut>0 ? Math.round(yemXarajat / jamiSut) : 0;

  document.getElementById('roi-result').innerHTML =
    '<div class="roi-grid">'
    + '<div class="roi-card roi-green"><div class="roi-icon">💰</div><div class="roi-label">'+tx('roi_yalpi')+'</div><div class="roi-val">'+fmtS(yalpiDaromad)+'</div><div class="roi-sub">'+jamiSut+'L × '+sotuvNarx.toLocaleString()+"</div></div>"
    + '<div class="roi-card roi-orange"><div class="roi-icon">🌾</div><div class="roi-label">'+tx('roi_xarajat')+'</div><div class="roi-val">'+fmtS(yemXarajat)+'</div><div class="roi-sub">'+tx('roi_yem_sarf')+'</div></div>'
    + '<div class="roi-card '+(sofFoyda>=0?'roi-blue':'roi-red')+'"><div class="roi-icon">'+(sofFoyda>=0?'📈':'📉')+'</div><div class="roi-label">'+tx('roi_foyda')+'</div><div class="roi-val">'+fmtS(sofFoyda)+'</div><div class="roi-sub">'+(sofFoyda>=0?tx('roi_foydali'):tx('roi_zarar'))+'</div></div>'
    + '<div class="roi-card roi-purple"><div class="roi-icon">🥛</div><div class="roi-label">'+tx('roi_tannarx')+'</div><div class="roi-val">'+fmtS(tannarx)+'</div><div class="roi-sub">'+tx('roi_per_litr')+'</div></div>'
    + '</div>';
}

// ── HISOBOT ──
var curRkFilter = 'all';
function renderRanking(filter){
  if(filter !== undefined) curRkFilter = filter;
  var dt=document.getElementById('rk-date');
  if(dt && !dt.value) dt.value=new Date().toISOString().slice(0,10);
  var date=dt?dt.value:new Date().toISOString().slice(0,10);
  var cows=ld('FM').filter(function(m){ return !m.sold && (m.tur==='Sigir'||m.tur==='Xonajin'); });
  var rows=[];
  cows.forEach(function(m){
    var rec=(m.sutOlchov||[]).filter(function(o){ return o.date===date; })[0];
    if(!rec) return;
    var tot=Math.round(((+rec.ert||0)+(+rec.obed||0)+(+rec.kech||0))*10)/10;
    rows.push({m:m, tot:tot});
  });
  rows.sort(function(a,b){ return b.tot-a.tot; });
  var g40=rows.filter(function(r){ return r.tot>=40; }).length;
  var g30=rows.filter(function(r){ return r.tot>=30 && r.tot<40; }).length;
  var g20=rows.filter(function(r){ return r.tot>=20 && r.tot<30; }).length;
  var gLt20=rows.filter(function(r){ return r.tot<20; }).length;
  var act={all:'',g40:'',g30:'',g20:'',lt20:''}; act[curRkFilter]=' rkon';
  var sg=document.getElementById('rk-sg');
  if(sg) sg.innerHTML=
    '<div class="sc gn rkcard'+act.g40+'" data-rk-filter="g40"><div class="si">🥇</div><div class="sv">'+g40+'</div><div class="sl">40+ L</div></div>'
    +'<div class="sc bl rkcard'+act.g30+'" data-rk-filter="g30"><div class="si">🥈</div><div class="sv">'+g30+'</div><div class="sl">30+ L</div></div>'
    +'<div class="sc am rkcard'+act.g20+'" data-rk-filter="g20"><div class="si">🥉</div><div class="sv">'+g20+'</div><div class="sl">20+ L</div></div>'
    +'<div class="sc rd rkcard'+act.lt20+'" data-rk-filter="lt20"><div class="si">⬇️</div><div class="sv">'+gLt20+'</div><div class="sl">20 L dan kam</div></div>';
  var flRows=rows;
  if(curRkFilter==='g40') flRows=rows.filter(function(r){ return r.tot>=40; });
  else if(curRkFilter==='g30') flRows=rows.filter(function(r){ return r.tot>=30 && r.tot<40; });
  else if(curRkFilter==='g20') flRows=rows.filter(function(r){ return r.tot>=20 && r.tot<30; });
  else if(curRkFilter==='lt20') flRows=rows.filter(function(r){ return r.tot<20; });
  var list=document.getElementById('rk-list'), empty=document.getElementById('rk-empty');
  if(!rows.length){ if(list) list.innerHTML=''; if(empty) empty.style.display='block'; return; }
  if(empty) empty.style.display='none';
  if(!flRows.length){ if(list) list.innerHTML='<div class="empty"><div class="ei">🥛</div><div class="et">'+tx('rk_filter_empty')+'</div></div>'; return; }
  var tico={Sigir:'🐄',Xonajin:'🐮'};
  var maxTot=rows[0].tot||1;
  if(list) list.innerHTML=flRows.map(function(r){
    var pos=rows.indexOf(r)+1, cls=pos===1?'top1':pos===2?'top2':pos===3?'top3':'';
    var medal=pos===1?'🥇':pos===2?'🥈':pos===3?'🥉':pos;
    var pct=Math.round(r.tot/maxTot*100);
    return '<div class="rkrow"><div class="rkpos '+cls+'">'+medal+'</div><div style="flex:1"><div style="display:flex;justify-content:space-between"><div class="rkname">'+(tico[r.m.tur]||'🐄')+' #'+esc(r.m.raqam)+'</div><div class="rkval">'+r.tot+' L</div></div><div class="rkbar"><div style="width:'+pct+'%"></div></div></div></div>';
  }).join('');
}
document.getElementById('rk-date').addEventListener('change', function(){ renderRanking('all'); });
document.getElementById('rk-sg').addEventListener('click', function(e){
  var c=e.target.closest('[data-rk-filter]'); if(!c) return;
  var f=c.getAttribute('data-rk-filter');
  renderRanking(curRkFilter===f ? 'all' : f);
});

function renderHis(){
  var mo=document.getElementById('his-m').value;
  var sr=ld('FS'); if(mo) sr=sr.filter(function(r){ return r.date.indexOf(mo)===0; });
  var yr=ld('FY'); if(mo) yr=yr.filter(function(r){ return r.date.indexOf(mo)===0; });
  var tS=0,tK=0,tT=0,tB=0,tY=0;
  sr.forEach(function(r){ tS+=r.litr;tK+=(r.komp||0);tT+=(r.tash||0);tB+=(r.buz||0); });
  yr.forEach(function(r){ tY+=r.jami; });
  var days={}; sr.forEach(function(r){ days[r.date]=1; });
  var dc=Object.keys(days).length;
  var ort=dc>0?Math.round(tS/dc):0;
  document.getElementById('his-sg').innerHTML=
    '<div class="sc gn"><div class="si">🥛</div><div class="sv">'+tS+'L</div><div class="sl">Jami sut</div></div>'
    +'<div class="sc bl"><div class="si">🏭</div><div class="sv">'+tK+'L</div><div class="sl">Kompaniya</div></div>'
    +'<div class="sc am"><div class="si">🛒</div><div class="sv">'+tT+'L</div><div class="sl">Tashqariga</div></div>'
    +'<div class="sc rd"><div class="si">🐮</div><div class="sv">'+tB+'L</div><div class="sl">Buzog\'lar</div></div>'
    +'<div class="sc pu"><div class="si">📈</div><div class="sv">'+ort+'L</div><div class="sl">O\'rtacha/kun</div></div>'
    +'<div class="sc tl"><div class="si">🌾</div><div class="sv">'+tY+'kg</div><div class="sl">Jami yem</div></div>';
  var bD={}; sr.forEach(function(r){ bD[r.date]=(bD[r.date]||0)+r.litr; });
  var ds=Object.keys(bD).sort().reverse();
  document.getElementById('his-sut').innerHTML=ds.length?
    '<div class="tw"><table><thead><tr><th>Sana</th><th>Litr</th></tr></thead><tbody>'
    +ds.map(function(d){ return '<tr><td>'+fd(d)+'</td><td><strong>'+bD[d]+'L</strong></td></tr>'; }).join('')
    +'</tbody></table></div>'
    :'<div class="empty"><div class="ei">🥛</div><div class="et">'+tx('yoq')+'</div></div>';
  var bT={}; yr.forEach(function(r){ bT[r.tur]=(bT[r.tur]||0)+r.jami; });
  document.getElementById('his-yem').innerHTML=Object.keys(bT).length?
    '<div class="tw"><table><thead><tr><th>Tur</th><th>Jami</th></tr></thead><tbody>'
    +Object.keys(bT).map(function(t){ return '<tr><td>'+esc(fName(t))+'</td><td><strong>'+bT[t]+'</strong></td></tr>'; }).join('')
    +'</tbody></table></div>'
    :'<div class="empty"><div class="ei">🌾</div><div class="et">'+tx('yoq')+'</div></div>';
  var sutDays=Object.keys(bD).sort();
  setTimeout(function(){ drawChart('sut-chart', sutDays, sutDays.map(function(d){ return bD[d]; }), '#52b788', 'sut-chart-empty'); }, 80);
  // O'rtacha sut (mol boshiga) = kunlik sut / kunlik mol soni
  var bMs={}; sr.forEach(function(r){ bMs[r.date]=(bMs[r.date]||0)+(r.ms||0); });
  var ortVals=sutDays.map(function(d){ return bMs[d]>0 ? +(bD[d]/bMs[d]).toFixed(1) : 0; });
  setTimeout(function(){ drawChart('ort-chart', sutDays, ortVals, '#8b5cf6', 'ort-chart-empty'); }, 80);
  var yD={}; yr.forEach(function(r){ yD[r.date]=(yD[r.date]||0)+r.jami; });
  var ydays=Object.keys(yD).sort();
  setTimeout(function(){ drawChart('yem-chart', ydays, ydays.map(function(d){ return yD[d]; }), '#e9a825', 'yem-chart-empty'); }, 80);
}

// ── GRAFIK ──
function drawChart(cid, labels, vals, color, emptyId){
  var canvas=document.getElementById(cid);
  var emptyEl=document.getElementById(emptyId);
  if(!canvas) return;
  if(!vals.length||vals.every(function(v){ return v===0; })){
    canvas.style.display='none'; if(emptyEl) emptyEl.style.display='block'; return;
  }
  canvas.style.display='block'; if(emptyEl) emptyEl.style.display='none';
  var dpr=window.devicePixelRatio||1;
  var W=canvas.parentElement.clientWidth-24;
  var H=parseInt(canvas.getAttribute('height'))||200;
  canvas.width=W*dpr; canvas.height=H*dpr;
  canvas.style.width=W+'px'; canvas.style.height=H+'px';
  var ctx=canvas.getContext('2d'); ctx.scale(dpr,dpr);
  var pad={top:20,right:10,bot:40,left:45};
  var cW=W-pad.left-pad.right, cH=H-pad.top-pad.bot;
  var max=Math.max.apply(null,vals)*1.15||1;
  var step=cW/(labels.length-1||1);
  ctx.strokeStyle='#e0ddd7'; ctx.lineWidth=1;
  for(var i=0;i<=4;i++){
    var y=pad.top+cH-(cH*i/4);
    ctx.beginPath(); ctx.moveTo(pad.left,y); ctx.lineTo(pad.left+cW,y); ctx.stroke();
    ctx.fillStyle='#6b7280'; ctx.font='11px sans-serif'; ctx.textAlign='right';
    ctx.fillText(Math.round(max*i/4),pad.left-6,y+4);
  }
  ctx.beginPath(); ctx.strokeStyle=color; ctx.lineWidth=2.5; ctx.lineJoin='round';
  vals.forEach(function(v,i){ var x=pad.left+i*step; var y=pad.top+cH-(cH*v/max); i===0?ctx.moveTo(x,y):ctx.lineTo(x,y); });
  ctx.stroke();
  ctx.beginPath();
  vals.forEach(function(v,i){ var x=pad.left+i*step; var y=pad.top+cH-(cH*v/max); i===0?ctx.moveTo(x,y):ctx.lineTo(x,y); });
  ctx.lineTo(pad.left+(vals.length-1)*step,pad.top+cH); ctx.lineTo(pad.left,pad.top+cH); ctx.closePath();
  ctx.fillStyle=color+'22'; ctx.fill();
  ctx.font='10px sans-serif'; ctx.textAlign='center';
  vals.forEach(function(v,i){
    var x=pad.left+i*step; var y=pad.top+cH-(cH*v/max);
    ctx.beginPath(); ctx.arc(x,y,3.5,0,Math.PI*2); ctx.fillStyle=color; ctx.fill();
    ctx.fillStyle='#6b7280'; ctx.fillText((labels[i]||'').slice(5),x,pad.top+cH+16);
  });
}

// ── HELPERS ──
function fd(d){ if(!d) return '—'; var p=d.split('-'); return p[2]+'.'+p[1]+'.'+p[0]; }
function fmtS(n){ return n.toLocaleString('uz-UZ')+" so'm"; }

// Sanaga oy qo'shish (manfiy ham bo'ladi)
function addMonths(dateStr, months){
  var d = new Date(dateStr);
  d.setMonth(d.getMonth() + months);
  return d.toISOString().slice(0,10);
}
function addDays(dateStr, days){
  var d = new Date(dateStr);
  d.setDate(d.getDate() + days);
  return d.toISOString().slice(0,10);
}
// Bo'g'ozlikning hozirgi oyini hisoblash (urug' qo'yilgan sanadan)
function hozirgiBogozOy(bogozBosh){
  if(!bogozBosh) return null;
  var bosh = new Date(bogozBosh);
  var hozir = new Date();
  var oylar = (hozir.getFullYear()-bosh.getFullYear())*12 + (hozir.getMonth()-bosh.getMonth());
  if(hozir.getDate() < bosh.getDate()) oylar--;
  if(oylar < 0) oylar = 0;
  if(oylar > 9) oylar = 9;
  return oylar;
}

function showTmpAlert(pgId){
  var p=document.getElementById(pgId); if(!p) return;
  var old=p.querySelector('.alert2'); if(old) old.remove();
  var d=document.createElement('div'); d.className='alert2';
  d.textContent='✅ '+tx('saqlandi');
  p.prepend(d); setTimeout(function(){ d.remove(); },2500);
}

// ── BILDIRISHNOMALAR ──
function nRead(){ var key = CU && CU.email ? 'FNREAD_' + CU.email : 'FNREAD'; return ldO(key); }
function nSaveRead(o){ var key = CU && CU.email ? 'FNREAD_' + CU.email : 'FNREAD'; sv(key, o); }
function buildNotifs(){
  var today = new Date().toISOString().slice(0,10);
  function dd(a,b){ return Math.round((Date.parse(b)-Date.parse(a))/86400000); }
  var N=[]; function add(pri,icon,title,text,sig,action){ N.push({pri:pri,icon:icon,title:title,text:text,sig:sig,action:action||null}); }
  var seen={};
  ld('FG').forEach(function(r){
    if(!r.key || r.tur==='Tugish') return;
    var k=r.mol+'|'+r.key+'|'+r.tur; if(seen[k]) return; seen[k]=1;
    var diff=dd(today,r.key);
    var nm=(r.tur==='Davolash')?'Davolash nazorati':"Emlash / ko'rik";
    var ic=(r.tur==='Davolash')?'💊':'💉';
    if(diff<0) add('red',ic,nm+" o'tib ketgan",'№'+esc(r.mol)+' — '+Math.abs(diff)+' kun avval ('+fd(r.key)+')','fg'+r.id);
    else if(diff===0) add('org',ic,'Bugun '+nm.toLowerCase(),'№'+esc(r.mol)+" ni ko'rikdan o'tkazing",'fg'+r.id);
    else if(diff<=5) add('blue',ic,nm+' yaqinlashmoqda','№'+esc(r.mol)+' — '+diff+' kundan keyin','fg'+r.id);
  });
  ld('FG').forEach(function(r){
    if(!r.recovered) return;
    var diff=dd(r.recovered,today);
    if(diff>=0 && diff<=7) add('blue','✅','Davolanishdan chiqdi','№'+esc(r.mol)+" "+'sog\'lom bo\'ldi ('+fd(r.recovered)+')','rec'+r.id);
  });
  ld('FM').forEach(function(m){
    if(!m.kutilgan) return;
    var diff=dd(today,m.kutilgan), id='tug'+(m.raqam||m.id||'')+m.kutilgan;
    if(diff<0) add('red','🐄',"Tug'ish muddati o'tdi",'№'+esc(m.raqam||'?')+' — kutilgan '+fd(m.kutilgan),id);
    else if(diff<=7) add('red','🐄',"Tug'ish juda yaqin",'№'+esc(m.raqam||'?')+' — atigi '+diff+' kun qoldi',id);
    else if(diff<=30) add('org','🐄',"Tug'ishga yaqin",'№'+esc(m.raqam||'?')+' — '+diff+' kun qoldi ('+fd(m.kutilgan)+')',id);
  });

  // Urug' qo'yilgandan 45 kun o'tgan — tekshiruv kerak
  ld('FM').forEach(function(m){
    if(m.holat!=='Tekshiruv' || !m.tekshirSana) return;
    var diff=dd(today,m.tekshirSana);
    if(diff<=0) add('red','🔍',"Bo'g'ozlik tekshiruvi kerak",'№'+esc(m.raqam||'?')+" — urug' qo'yilganiga 45 kun bo'ldi. Doktor tekshirsin.",'chk'+m.id,{type:'check',mid:m.id});
  });
  // Bo'g'ozlik 7-oyga yetgan — sutdan chiqarish vaqti
  ld('FM').forEach(function(m){
    if(m.holat!=='Bogoz' || !m.bogozBosh || m.quritilgan) return;
    var oy=hozirgiBogozOy(m.bogozBosh);
    if(oy>=7) add('org','🥛',"Sutdan chiqarish vaqti",'№'+esc(m.raqam||'?')+' — bo\'g\'ozlik 7-oyga yetdi. Sog\'ishni tugating.','dry'+m.id,{type:'dry',mid:m.id});
  });
  // Tug'gandan keyin kolostrum davri tugadi
  ld('FM').forEach(function(m){
    if(!m.tugSana) return;
    var diff=dd(m.tugSana,today);
    if(diff===10) add('blue','🥛',"Toza sut boshlandi",'№'+esc(m.raqam||'?')+" — tug'ganiga 10 kun bo'ldi, sut endi hisobga kiradi",'kol'+m.id+m.tugSana);
  });
  var mo=today.slice(0,7), kelMap={}, sarfMap={}, brMap={};
  ld('FK').filter(function(r){return r.date.indexOf(mo)===0;}).forEach(function(r){ kelMap[r.tur]=(kelMap[r.tur]||0)+r.miq; brMap[r.tur]=r.br; });
  ld('FY').filter(function(r){return r.date.indexOf(mo)===0;}).forEach(function(r){ sarfMap[r.tur]=(sarfMap[r.tur]||0)+r.jami; if(!brMap[r.tur])brMap[r.tur]=r.u||'kg'; });
  var allT={}; Object.keys(kelMap).forEach(function(k){allT[k]=1;}); Object.keys(sarfMap).forEach(function(k){allT[k]=1;});
  Object.keys(allT).forEach(function(tur){
    if((brMap[tur]||'kg')!=='kg') return;
    if((kelMap[tur]||0)<=0) return;
    var qol=(kelMap[tur]||0)-(sarfMap[tur]||0);
    if(qol<200) add('red','📦','Yem tugayapti','"'+fName(tur)+'" — atigi '+qol+' kg qoldi','inv'+tur+mo);
    else if(qol<500) add('org','📦','Yem zaxirasi kam','"'+fName(tur)+'" — '+qol+' kg qoldi','inv'+tur+mo);
  });
  var ord={red:0,org:1,blue:2}; N.sort(function(a,b){return ord[a.pri]-ord[b.pri];});
  return N;
}
var _notifs=[];
function refreshNotifs(){
  _notifs=buildNotifs();
  var read=nRead(), un=0; _notifs.forEach(function(n){ if(!read[n.sig]) un++; });
  var badge=document.getElementById('nbadge'), bell=document.getElementById('nbell');
  if(badge){ if(un>0){ badge.textContent=un>9?'9+':un; badge.classList.add('on'); if(bell){ bell.classList.add('ring'); setTimeout(function(){bell.classList.remove('ring');},1000); } } else { badge.classList.remove('on'); } }
}
function renderNpanel(){
  var read=nRead(), list=document.getElementById('nlist'); if(!list) return;
  if(!_notifs.length){ list.innerHTML='<div class="nempty"><div class="e">✅</div><div class="t">'+tx('hammasi_joyida')+'</div><div class="s">'+tx('diqqat_yoq')+'</div></div>'; return; }
  var icCls={red:'ni-red',org:'ni-org',blue:'ni-blue'};
  list.innerHTML=_notifs.map(function(n){ var unread=!read[n.sig];
    var act='';
    if(n.action && n.action.type==='check' && CU.role!=='molboqar'){
      act='<div class="nact"><button class="nbtn ok" data-check-yes="'+n.action.mid+'">✅ '+tx('bogoz_ha')+'</button><button class="nbtn no" data-check-no="'+n.action.mid+'">❌ '+tx('bogoz_yoq')+'</button></div>';
    } else if(n.action && n.action.type==='dry' && CU.role!=='molboqar'){
      act='<div class="nact"><button class="nbtn ok" data-dry-off="'+n.action.mid+'">🥛 '+tx('sutdan_chiqarildi')+'</button></div>';
    }
    return '<div class="nitem'+(unread?' unread':'')+'"><div class="nicon '+icCls[n.pri]+'">'+n.icon+'</div><div class="ntxt"><div class="ntt">'+n.title+'</div><div class="nts">'+n.text+'</div>'+act+'</div>'+(unread?'<div class="nidot"></div>':'')+'</div>'; }).join('');
}
function molVoqeaTasdiqla(mid, natija){
  var mols=ld('FM'), m=null;
  for(var i=0;i<mols.length;i++){ if(mols[i].id===mid){ m=mols[i]; break; } }
  if(!m) return;
  if(natija==='ha'){
    m.holat='Bogoz'; m.bogozBosh=m.urugSana; m.kutilgan=addMonths(m.urugSana,9);
    m.tekshirSana=''; 
  } else {
    m.holat='Soglom'; m.urugSana=''; m.tekshirSana='';
  }
  sv('FM',mols); refreshNotifs(); renderNpanel(); renderMolGrid();
  alert(natija==='ha'?tx('bogoz_tasdiqlandi'):tx('bogoz_bekor'));
}
function molSutdanChiqar(mid){
  var mols=ld('FM'), m=null;
  for(var i=0;i<mols.length;i++){ if(mols[i].id===mid){ m=mols[i]; break; } }
  if(!m) return;
  m.quritilgan=new Date().toISOString().slice(0,10);
  sv('FM',mols); refreshNotifs(); renderNpanel(); renderMolGrid();
  alert(tx('sutdan_chiqarildi_msg'));
}
(function(){
  var np=document.getElementById('nlist');
  if(np) np.addEventListener('click', function(e){
    var y=e.target.closest('[data-check-yes]'); if(y){ molVoqeaTasdiqla(parseFloat(y.getAttribute('data-check-yes')),'ha'); return; }
    var n=e.target.closest('[data-check-no]'); if(n){ molVoqeaTasdiqla(parseFloat(n.getAttribute('data-check-no')),'yoq'); return; }
    var d=e.target.closest('[data-dry-off]'); if(d){ molSutdanChiqar(parseFloat(d.getAttribute('data-dry-off'))); return; }
  });
})();
function openNpanel(){ refreshNotifs(); renderNpanel(); document.getElementById('nmask').classList.add('on'); document.getElementById('npanel').classList.add('on'); var read=nRead(); _notifs.forEach(function(n){ read[n.sig]=1; }); nSaveRead(read); var bd=document.getElementById('nbadge'); if(bd) bd.classList.remove('on'); }
function closeNpanel(){ document.getElementById('nmask').classList.remove('on'); document.getElementById('npanel').classList.remove('on'); }
function markAllRead(){ var read=nRead(); _notifs.forEach(function(n){ read[n.sig]=1; }); nSaveRead(read); refreshNotifs(); renderNpanel(); }
function nToast(){
  var read=nRead(), urg=_notifs.filter(function(n){ return (n.pri==='red'||n.pri==='org') && !read[n.sig]; }).slice(0,3), box=document.getElementById('ntoasts'); if(!box) return;
  urg.forEach(function(n,i){ setTimeout(function(){ var el=document.createElement('div'); el.className='ntoast'+(n.pri==='org'?' org':''); el.innerHTML='<div class="ic">'+n.icon+'</div><div><div class="tt">'+n.title+'</div><div class="ts">'+n.text+'</div></div>'; box.appendChild(el); setTimeout(function(){ el.style.transition='opacity .3s,transform .3s'; el.style.opacity='0'; el.style.transform='translateY(14px)'; setTimeout(function(){el.remove();},320); },4500); }, i*800); });
}
(function(){
  var bell=document.getElementById('nbell'); if(bell) bell.addEventListener('click', function(e){ e.stopPropagation(); openNpanel(); });
  var mask=document.getElementById('nmask'); if(mask) mask.addEventListener('click', closeNpanel);
  var clr=document.getElementById('nclr'); if(clr) clr.addEventListener('click', markAllRead);
})();

// ── EKSPORT: PDF (chop etish) + EXCEL (CSV) ──
function isIOS(){ return /iPad|iPhone|iPod/.test(navigator.userAgent) || (navigator.platform==='MacIntel' && navigator.maxTouchPoints>1); }
function expMonth(){ var e=document.getElementById('his-m'); return (e&&e.value) ? e.value : new Date().toISOString().slice(0,10).slice(0,7); }
function repBirlikNarx(){
  var kelAgg={}, bn={};
  ld('FK').forEach(function(r){ if(!kelAgg[r.tur]) kelAgg[r.tur]={s:0,kg:0}; var s=(r.jamiNarx!=null)?r.jamiNarx:((r.narx||0)*(r.miq||0)); var kg=(r.miqKg!=null)?r.miqKg:(r.br==='tonna'?(r.miq||0)*1000:(r.miq||0)); kelAgg[r.tur].s+=s; kelAgg[r.tur].kg+=kg; });
  Object.keys(kelAgg).forEach(function(t){ bn[t]=kelAgg[t].kg>0?kelAgg[t].s/kelAgg[t].kg:0; });
  return bn;
}
function repData(mo){
  var sr=ld('FS').filter(function(r){return !mo||r.date.indexOf(mo)===0;});
  var yr=ld('FY').filter(function(r){return !mo||r.date.indexOf(mo)===0;});
  var tS=0,tK=0,tT=0,tB=0,tI=0,tY=0,days={},bD={},bT={},bTu={};
  sr.forEach(function(r){ tS+=r.litr;tK+=(r.komp||0);tT+=(r.tash||0);tB+=(r.buz||0);tI+=(r.ishchi||0);days[r.date]=1;bD[r.date]=(bD[r.date]||0)+r.litr; });
  yr.forEach(function(r){ tY+=r.jami; bT[r.tur]=(bT[r.tur]||0)+r.jami; bTu[r.tur]=r.u||'kg'; });
  var dc=Object.keys(days).length||0;
  var cows=ld('FM').filter(function(m){return m.tur==='Sigir'||m.tur==='Xonajin';}).length;
  var ne=document.getElementById('narx'); var narx=ne?(parseFloat(ne.value)||0):0;
  var price = narx>0 ? narx : 4500;
  var yalpi=(tK+tT)*price;
  var bn=repBirlikNarx(), xarajat=0; yr.forEach(function(r){ xarajat+=(r.jami||0)*(bn[r.tur]||0); }); xarajat=Math.round(xarajat);
  var sof=yalpi-xarajat, tannarx=tS>0?Math.round(xarajat/tS):0;
  var ortBosh=(dc>0&&cows>0)?(tS/dc/cows).toFixed(1):0;
  return {tS:tS,tK:tK,tT:tT,tB:tB,tI:tI,tY:tY,dc:dc,ort:dc>0?Math.round(tS/dc):0,ortBosh:ortBosh,cows:cows,price:price,yalpi:yalpi,xarajat:xarajat,sof:sof,tannarx:tannarx,bD:bD,bT:bT,bTu:bTu};
}
function repChartSVG(bD){
  var ds=Object.keys(bD).sort(), vals=ds.map(function(d){return bD[d];});
  if(!vals.length) return '<div style="color:#94a3b8;font-size:12px;padding:10px">Ma\'lumot yo\'q</div>';
  var W=720,H=220,pad={l:46,r:14,t:14,b:28},iw=W-pad.l-pad.r,ih=H-pad.t-pad.b;
  var max=Math.max.apply(null,vals)*1.12||1, n=vals.length, stepX=n>1?iw/(n-1):0;
  var pts=vals.map(function(v,i){ return {x:pad.l+i*stepX, y:pad.t+ih-(v/max*ih)}; });
  var line='M'+pts.map(function(p){return p.x.toFixed(1)+','+p.y.toFixed(1);}).join(' L');
  var area=line+' L'+pts[pts.length-1].x.toFixed(1)+','+(pad.t+ih)+' L'+pts[0].x.toFixed(1)+','+(pad.t+ih)+' Z';
  var grid='',yl='';
  for(var i=0;i<=4;i++){ var gy=pad.t+ih-(ih*i/4); grid+='<line x1="'+pad.l+'" y1="'+gy.toFixed(1)+'" x2="'+(W-pad.r)+'" y2="'+gy.toFixed(1)+'" stroke="#e5e7eb"/>'; yl+='<text x="'+(pad.l-6)+'" y="'+(gy+4).toFixed(1)+'" font-size="11" fill="#94a3b8" text-anchor="end">'+Math.round(max*i/4)+'</text>'; }
  var dots=pts.map(function(p){return '<circle cx="'+p.x.toFixed(1)+'" cy="'+p.y.toFixed(1)+'" r="3" fill="#2d6a4f"/>';}).join('');
  var xl=ds.map(function(d,i){ if(n>12 && (i%2)) return ''; return '<text x="'+pts[i].x.toFixed(1)+'" y="'+(pad.t+ih+16)+'" font-size="9" fill="#94a3b8" text-anchor="middle">'+d.slice(5)+'</text>'; }).join('');
  return '<svg viewBox="0 0 '+W+' '+H+'" style="width:100%;height:auto"><defs><linearGradient id="rg" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#52b788" stop-opacity=".35"/><stop offset="100%" stop-color="#52b788" stop-opacity="0"/></linearGradient></defs>'+grid+yl+'<path d="'+area+'" fill="url(#rg)"/><path d="'+line+'" fill="none" stroke="#2d6a4f" stroke-width="2.2"/>'+dots+xl+'</svg>';
}
function rr(k,v){ return '<div class="rep-r"><span class="k">'+k+'</span><span class="v">'+v+'</span></div>'; }
function repSub(t){ return '<div style="font-weight:700;margin:9px 0 3px;color:#15803d;font-size:13px">'+t+'</div>'; }
function buildReportHTML(){
  var mo=expMonth(), D=repData(mo), now=new Date(), today=now.toISOString().slice(0,10);
  var farm=(ldO('FSET').name)||'Akramjon Olimjon Obod F/X';
  var sick=ld('FM').filter(function(m){ return m.holat==='Kasal'||m.holat==='Davolashda'; });
  var preg=ld('FM').filter(function(m){ return m.kutilgan; });
  var fg=ld('FG').filter(function(r){ return !mo || (r.date||'').indexOf(mo)===0; });
  var births=fg.filter(function(r){return r.tur==='Tugish';});
  var treats=fg.filter(function(r){return r.tur==='Davolash';});
  var vacc=fg.filter(function(r){return r.tur==='Emlash';});
  var recd=ld('FG').filter(function(r){ return r.recovered && (!mo || (r.recovered||'').indexOf(mo)===0); });
  function hol(x){ return x==='Davolashda'?tx('davolashda'):tx('kasal'); }
  var h='<div class="rep">';
  h+='<div class="rep-h"><div class="lg">🐄</div><div><h1>'+farm+'</h1><div class="s">'+tx('rep_title')+' — '+(mo||tx('rep_all'))+'</div></div><div class="d">'+fd(today)+'<br>'+now.toTimeString().slice(0,5)+'</div></div>';
  h+='<div class="rep-s"><h2>1. '+tx('rep_s1')+'</h2>'+rr(tx('rep_jami_sut'),D.tS+' L')+rr(tx('rep_komp'),D.tK+' L')+rr(tx('rep_tash'),D.tT+' L')+rr(tx('rep_buz'),D.tB+' L')+rr(tx('rep_ish'),D.tI+' L')+rr(tx('rep_ort_kun'),D.ort+' L')+rr(tx('rep_ort_bosh'),D.ortBosh+' L')+rr(tx('rep_faol'),D.dc+' '+tx('rep_kun'))+'<div style="margin-top:10px">'+repChartSVG(D.bD)+'</div></div>';
  var yemRows=Object.keys(D.bT).map(function(t){return rr(fName(t),D.bT[t]+' '+uName(D.bTu[t]));}).join('')||rr(tx('rep_yoq'),'0');
  h+='<div class="rep-s"><h2>2. '+tx('rep_s2')+'</h2>'+rr(tx('rep_jami_yem'),D.tY+' '+uName('kg'))+yemRows+'</div>';
  h+='<div class="rep-s"><h2>3. '+tx('rep_s3')+'</h2>'+rr(tx('rep_narx'),fmtS(D.price))+rr(tx('rep_yalpi'),fmtS(D.yalpi))+rr(tx('rep_xarajat'),fmtS(D.xarajat))+rr(tx('rep_sof'),fmtS(D.sof))+rr(tx('rep_tannarx'),fmtS(D.tannarx))+'</div>';
  var ev=repSub('🐣 '+tx('rep_tugilgan'))+( births.length?births.map(function(r){return rr('№'+esc(r.mol)+' → '+(r.tSon||1)+' '+tx('rep_ta')+(r.tRaqlar?' (№'+esc(r.tRaqlar)+')':''),fd(r.date));}).join(''):rr(tx('rep_yoq'),'0') );
  ev+=repSub('💊 '+tx('rep_davolangan'))+( treats.length?treats.map(function(r){return rr('№'+esc(r.mol)+(r.dori?' · '+esc(r.dori):''),fd(r.date)+(r.recovered?' ✅':''));}).join(''):rr(tx('rep_yoq'),'0') );
  ev+=repSub('✅ '+tx('rep_sogaygan'))+( recd.length?recd.map(function(r){return rr('№'+esc(r.mol),fd(r.recovered));}).join(''):rr(tx('rep_yoq'),'0') );
  ev+=repSub('💉 '+tx('rep_emlangan'))+( vacc.length?vacc.map(function(r){return rr('№'+esc(r.mol)+(r.dori?' · '+esc(r.dori):''),fd(r.date));}).join(''):rr(tx('rep_yoq'),'0') );
  h+='<div class="rep-s"><h2>4. '+tx('rep_s5')+'</h2>'+ev+'</div>';
  var heal=repSub('⚠️ '+tx('rep_kasal'))+( sick.length?sick.map(function(m){return rr('№'+esc(m.raqam||'?'),hol(m.holat));}).join(''):rr(tx('rep_yoq'),'0') );
  heal+=repSub('🤰 '+tx('rep_kutil'))+( preg.length?preg.map(function(m){var df=Math.round((Date.parse(m.kutilgan)-Date.now())/86400000);return rr('№'+esc(m.raqam||'?'),fd(m.kutilgan)+(df>=0?' ('+df+' '+tx('rep_kun')+')':' ('+tx('rep_otdi')+')'));}).join(''):rr(tx('rep_yoq'),'0') );
  h+='<div class="rep-s"><h2>5. '+tx('rep_s4')+'</h2>'+heal+'</div>';
  h+='<div class="rep-f"><span>'+tx('rep_gen')+' — '+fd(today)+'</span><span>'+farm+'</span></div></div>';
  return h;
}
function exportPDF(){
  var area=document.getElementById('printArea');
  area.innerHTML='<div class="repbar"><button class="pr" onclick="doPrint()">🖨 Chop etish / PDF saqlash</button><button class="cl" onclick="closeReport()">✕ Yopish</button></div>'+buildReportHTML();
  area.style.display='block';
  document.body.classList.add('reporting');
  window.scrollTo(0,0);
}
function doPrint(){ document.body.classList.add('printing'); setTimeout(function(){ try{ window.print(); }catch(e){} },200); }
function closeReport(){ var a=document.getElementById('printArea'); if(a){ a.style.display='none'; a.innerHTML=''; } document.body.classList.remove('reporting'); document.body.classList.remove('printing'); }
window.addEventListener('afterprint', function(){ document.body.classList.remove('printing'); });

function exportExcel(){
  var mo=expMonth(), D=repData(mo), now=new Date(), farm=(ldO('FSET').name)||'A.O. Obod', rows=[];
  rows.push(['FERMA HISOBOTI']); rows.push([farm]); rows.push(['Davr', mo||'Barchasi']); rows.push(['Yaratilgan', fd(now.toISOString().slice(0,10))]); rows.push([]);
  rows.push(['SUT']); rows.push(['Korsatkich','Qiymat']); rows.push(['Jami sut (L)',D.tS]); rows.push(['Kompaniyaga (L)',D.tK]); rows.push(['Tashqariga (L)',D.tT]); rows.push(['Buzoqlarga (L)',D.tB]); rows.push(["O'rtacha/kun (L)",D.ort]); rows.push(['Ishchilarga (L)',D.tI]); rows.push(["O'rtacha 1 bosh/kun (L)",D.ortBosh]); rows.push([]);
  rows.push(['KUNLIK SUT']); rows.push(['Sana','Litr']); Object.keys(D.bD).sort().forEach(function(d){ rows.push([fd(d), D.bD[d]]); }); rows.push([]);
  rows.push(['YEM TURLARI']); rows.push(['Tur','Jami (kg)']); Object.keys(D.bT).forEach(function(t){ rows.push([t, D.bT[t]]); }); rows.push([]);
  rows.push(['DAROMAD']); rows.push(['1 litr narxi',D.price]); rows.push(['Yalpi daromad',D.yalpi]); rows.push(['Yem xarajati',D.xarajat]); rows.push(['Sof foyda',D.sof]); rows.push(['1L tannarx',D.tannarx]); rows.push([]);
  var fgX=ld('FG').filter(function(r){return !mo||(r.date||'').indexOf(mo)===0;});
  rows.push(['VOQEALAR']); rows.push(['Tugilgan buzoqlar']); rows.push(['Ona','Soni','Raqamlar','Sana']);
  fgX.filter(function(r){return r.tur==='Tugish';}).forEach(function(r){ rows.push(['#'+r.mol,(r.tSon||1),r.tRaqlar||'',fd(r.date)]); });
  rows.push([]); rows.push(['Davolanganlar']); rows.push(['Mol','Dori','Sana','Holat']);
  fgX.filter(function(r){return r.tur==='Davolash';}).forEach(function(r){ rows.push(['#'+r.mol,r.dori||'',fd(r.date),r.recovered?'Sogaygan':'Davolanmoqda']); });
  rows.push([]); rows.push(['Emlanganlar']); rows.push(['Mol','Dori','Sana']);
  fgX.filter(function(r){return r.tur==='Emlash';}).forEach(function(r){ rows.push(['#'+r.mol,r.dori||'',fd(r.date)]); });
  var csv=rows.map(function(r){ return r.map(function(c){ return '"'+String(c==null?'':c).replace(/"/g,'""')+'"'; }).join(','); }).join('\r\n');
  var fname='ferma-hisobot-'+(mo||'all')+'.csv';
  var blob=new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8;'});
  try{
    if(navigator.share && typeof File!=='undefined'){
      var file=new File(['\ufeff'+csv], fname, {type:'text/csv'});
      if(!navigator.canShare || navigator.canShare({files:[file]})){
        navigator.share({files:[file], title:'Ferma hisoboti'}).catch(function(err){ if(err && err.name==='AbortError') return; csvFallback(csv); });
        return;
      }
    }
  }catch(e){}
  try{
    var a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=fname;
    document.body.appendChild(a); a.click(); document.body.removeChild(a);
    return;
  }catch(e){}
  csvFallback(csv);
}
function csvFallback(csv){
  var area=document.getElementById('printArea');
  area.innerHTML='<div class="repbar"><button class="cl" onclick="closeReport()">✕ Yopish</button></div>'
    +'<p style="font-size:13px;color:#333;margin:0 0 8px">Matnni belgilab nusxalang, so\'ng Excel/Numbers\'ga joylang:</p>'
    +'<textarea style="width:100%;height:68vh;font-size:12px;font-family:monospace;border:1px solid #ccc;border-radius:8px;padding:10px" onclick="this.select()">'+csv.replace(/</g,'&lt;')+'</textarea>';
  area.style.display='block'; document.body.classList.add('reporting'); window.scrollTo(0,0);
}
(function(){
  var p=document.getElementById('exp-pdf'); if(p) p.addEventListener('click', exportPDF);
  var x=document.getElementById('exp-xls'); if(x) x.addEventListener('click', exportExcel);
})();

// ── INIT ──
try{ var fl=localStorage.getItem('FL'); if(fl==='kir') setLang('kir'); }catch(e){}
applyLang();
boot();
setInterval(function() {
  if (typeof CU !== 'undefined' && CU) refreshNotifs();
}, 60000);
